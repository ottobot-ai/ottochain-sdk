// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.2
//   protoc               unknown
// source: ottochain/apps/governance/v1/governance.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Struct } from "../../../../google/protobuf/struct.js";
import { Timestamp } from "../../../../google/protobuf/timestamp.js";
import { Address } from "../../../v1/common.js";

export const protobufPackage = "ottochain.apps.governance.v1";

/** Type of DAO governance model */
export enum DAOType {
  DAO_TYPE_UNSPECIFIED = "DAO_TYPE_UNSPECIFIED",
  /** DAO_TYPE_SINGLE - Single owner controls all actions */
  DAO_TYPE_SINGLE = "DAO_TYPE_SINGLE",
  /** DAO_TYPE_MULTISIG - N-of-M signatures required */
  DAO_TYPE_MULTISIG = "DAO_TYPE_MULTISIG",
  /** DAO_TYPE_TOKEN - Token-weighted voting */
  DAO_TYPE_TOKEN = "DAO_TYPE_TOKEN",
  /** DAO_TYPE_THRESHOLD - Reputation threshold for participation */
  DAO_TYPE_THRESHOLD = "DAO_TYPE_THRESHOLD",
  UNRECOGNIZED = "UNRECOGNIZED",
}

export function dAOTypeFromJSON(object: any): DAOType {
  switch (object) {
    case 0:
    case "DAO_TYPE_UNSPECIFIED":
      return DAOType.DAO_TYPE_UNSPECIFIED;
    case 1:
    case "DAO_TYPE_SINGLE":
      return DAOType.DAO_TYPE_SINGLE;
    case 2:
    case "DAO_TYPE_MULTISIG":
      return DAOType.DAO_TYPE_MULTISIG;
    case 3:
    case "DAO_TYPE_TOKEN":
      return DAOType.DAO_TYPE_TOKEN;
    case 4:
    case "DAO_TYPE_THRESHOLD":
      return DAOType.DAO_TYPE_THRESHOLD;
    case -1:
    case "UNRECOGNIZED":
    default:
      return DAOType.UNRECOGNIZED;
  }
}

export function dAOTypeToJSON(object: DAOType): string {
  switch (object) {
    case DAOType.DAO_TYPE_UNSPECIFIED:
      return "DAO_TYPE_UNSPECIFIED";
    case DAOType.DAO_TYPE_SINGLE:
      return "DAO_TYPE_SINGLE";
    case DAOType.DAO_TYPE_MULTISIG:
      return "DAO_TYPE_MULTISIG";
    case DAOType.DAO_TYPE_TOKEN:
      return "DAO_TYPE_TOKEN";
    case DAOType.DAO_TYPE_THRESHOLD:
      return "DAO_TYPE_THRESHOLD";
    case DAOType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export function dAOTypeToNumber(object: DAOType): number {
  switch (object) {
    case DAOType.DAO_TYPE_UNSPECIFIED:
      return 0;
    case DAOType.DAO_TYPE_SINGLE:
      return 1;
    case DAOType.DAO_TYPE_MULTISIG:
      return 2;
    case DAOType.DAO_TYPE_TOKEN:
      return 3;
    case DAOType.DAO_TYPE_THRESHOLD:
      return 4;
    case DAOType.UNRECOGNIZED:
    default:
      return -1;
  }
}

/** DAO lifecycle status */
export enum DAOStatus {
  DAO_STATUS_UNSPECIFIED = "DAO_STATUS_UNSPECIFIED",
  DAO_STATUS_ACTIVE = "DAO_STATUS_ACTIVE",
  DAO_STATUS_DISSOLVED = "DAO_STATUS_DISSOLVED",
  UNRECOGNIZED = "UNRECOGNIZED",
}

export function dAOStatusFromJSON(object: any): DAOStatus {
  switch (object) {
    case 0:
    case "DAO_STATUS_UNSPECIFIED":
      return DAOStatus.DAO_STATUS_UNSPECIFIED;
    case 1:
    case "DAO_STATUS_ACTIVE":
      return DAOStatus.DAO_STATUS_ACTIVE;
    case 2:
    case "DAO_STATUS_DISSOLVED":
      return DAOStatus.DAO_STATUS_DISSOLVED;
    case -1:
    case "UNRECOGNIZED":
    default:
      return DAOStatus.UNRECOGNIZED;
  }
}

export function dAOStatusToJSON(object: DAOStatus): string {
  switch (object) {
    case DAOStatus.DAO_STATUS_UNSPECIFIED:
      return "DAO_STATUS_UNSPECIFIED";
    case DAOStatus.DAO_STATUS_ACTIVE:
      return "DAO_STATUS_ACTIVE";
    case DAOStatus.DAO_STATUS_DISSOLVED:
      return "DAO_STATUS_DISSOLVED";
    case DAOStatus.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export function dAOStatusToNumber(object: DAOStatus): number {
  switch (object) {
    case DAOStatus.DAO_STATUS_UNSPECIFIED:
      return 0;
    case DAOStatus.DAO_STATUS_ACTIVE:
      return 1;
    case DAOStatus.DAO_STATUS_DISSOLVED:
      return 2;
    case DAOStatus.UNRECOGNIZED:
    default:
      return -1;
  }
}

/** Proposal status for voting DAOs */
export enum ProposalStatus {
  PROPOSAL_STATUS_UNSPECIFIED = "PROPOSAL_STATUS_UNSPECIFIED",
  PROPOSAL_STATUS_PENDING = "PROPOSAL_STATUS_PENDING",
  PROPOSAL_STATUS_VOTING = "PROPOSAL_STATUS_VOTING",
  PROPOSAL_STATUS_QUEUED = "PROPOSAL_STATUS_QUEUED",
  PROPOSAL_STATUS_EXECUTED = "PROPOSAL_STATUS_EXECUTED",
  PROPOSAL_STATUS_REJECTED = "PROPOSAL_STATUS_REJECTED",
  PROPOSAL_STATUS_CANCELLED = "PROPOSAL_STATUS_CANCELLED",
  UNRECOGNIZED = "UNRECOGNIZED",
}

export function proposalStatusFromJSON(object: any): ProposalStatus {
  switch (object) {
    case 0:
    case "PROPOSAL_STATUS_UNSPECIFIED":
      return ProposalStatus.PROPOSAL_STATUS_UNSPECIFIED;
    case 1:
    case "PROPOSAL_STATUS_PENDING":
      return ProposalStatus.PROPOSAL_STATUS_PENDING;
    case 2:
    case "PROPOSAL_STATUS_VOTING":
      return ProposalStatus.PROPOSAL_STATUS_VOTING;
    case 3:
    case "PROPOSAL_STATUS_QUEUED":
      return ProposalStatus.PROPOSAL_STATUS_QUEUED;
    case 4:
    case "PROPOSAL_STATUS_EXECUTED":
      return ProposalStatus.PROPOSAL_STATUS_EXECUTED;
    case 5:
    case "PROPOSAL_STATUS_REJECTED":
      return ProposalStatus.PROPOSAL_STATUS_REJECTED;
    case 6:
    case "PROPOSAL_STATUS_CANCELLED":
      return ProposalStatus.PROPOSAL_STATUS_CANCELLED;
    case -1:
    case "UNRECOGNIZED":
    default:
      return ProposalStatus.UNRECOGNIZED;
  }
}

export function proposalStatusToJSON(object: ProposalStatus): string {
  switch (object) {
    case ProposalStatus.PROPOSAL_STATUS_UNSPECIFIED:
      return "PROPOSAL_STATUS_UNSPECIFIED";
    case ProposalStatus.PROPOSAL_STATUS_PENDING:
      return "PROPOSAL_STATUS_PENDING";
    case ProposalStatus.PROPOSAL_STATUS_VOTING:
      return "PROPOSAL_STATUS_VOTING";
    case ProposalStatus.PROPOSAL_STATUS_QUEUED:
      return "PROPOSAL_STATUS_QUEUED";
    case ProposalStatus.PROPOSAL_STATUS_EXECUTED:
      return "PROPOSAL_STATUS_EXECUTED";
    case ProposalStatus.PROPOSAL_STATUS_REJECTED:
      return "PROPOSAL_STATUS_REJECTED";
    case ProposalStatus.PROPOSAL_STATUS_CANCELLED:
      return "PROPOSAL_STATUS_CANCELLED";
    case ProposalStatus.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export function proposalStatusToNumber(object: ProposalStatus): number {
  switch (object) {
    case ProposalStatus.PROPOSAL_STATUS_UNSPECIFIED:
      return 0;
    case ProposalStatus.PROPOSAL_STATUS_PENDING:
      return 1;
    case ProposalStatus.PROPOSAL_STATUS_VOTING:
      return 2;
    case ProposalStatus.PROPOSAL_STATUS_QUEUED:
      return 3;
    case ProposalStatus.PROPOSAL_STATUS_EXECUTED:
      return 4;
    case ProposalStatus.PROPOSAL_STATUS_REJECTED:
      return 5;
    case ProposalStatus.PROPOSAL_STATUS_CANCELLED:
      return 6;
    case ProposalStatus.UNRECOGNIZED:
    default:
      return -1;
  }
}

/** Vote choice */
export enum VoteChoice {
  VOTE_CHOICE_UNSPECIFIED = "VOTE_CHOICE_UNSPECIFIED",
  VOTE_CHOICE_FOR = "VOTE_CHOICE_FOR",
  VOTE_CHOICE_AGAINST = "VOTE_CHOICE_AGAINST",
  VOTE_CHOICE_ABSTAIN = "VOTE_CHOICE_ABSTAIN",
  UNRECOGNIZED = "UNRECOGNIZED",
}

export function voteChoiceFromJSON(object: any): VoteChoice {
  switch (object) {
    case 0:
    case "VOTE_CHOICE_UNSPECIFIED":
      return VoteChoice.VOTE_CHOICE_UNSPECIFIED;
    case 1:
    case "VOTE_CHOICE_FOR":
      return VoteChoice.VOTE_CHOICE_FOR;
    case 2:
    case "VOTE_CHOICE_AGAINST":
      return VoteChoice.VOTE_CHOICE_AGAINST;
    case 3:
    case "VOTE_CHOICE_ABSTAIN":
      return VoteChoice.VOTE_CHOICE_ABSTAIN;
    case -1:
    case "UNRECOGNIZED":
    default:
      return VoteChoice.UNRECOGNIZED;
  }
}

export function voteChoiceToJSON(object: VoteChoice): string {
  switch (object) {
    case VoteChoice.VOTE_CHOICE_UNSPECIFIED:
      return "VOTE_CHOICE_UNSPECIFIED";
    case VoteChoice.VOTE_CHOICE_FOR:
      return "VOTE_CHOICE_FOR";
    case VoteChoice.VOTE_CHOICE_AGAINST:
      return "VOTE_CHOICE_AGAINST";
    case VoteChoice.VOTE_CHOICE_ABSTAIN:
      return "VOTE_CHOICE_ABSTAIN";
    case VoteChoice.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export function voteChoiceToNumber(object: VoteChoice): number {
  switch (object) {
    case VoteChoice.VOTE_CHOICE_UNSPECIFIED:
      return 0;
    case VoteChoice.VOTE_CHOICE_FOR:
      return 1;
    case VoteChoice.VOTE_CHOICE_AGAINST:
      return 2;
    case VoteChoice.VOTE_CHOICE_ABSTAIN:
      return 3;
    case VoteChoice.UNRECOGNIZED:
    default:
      return -1;
  }
}

/** DAO metadata */
export interface DAOMetadata {
  description: string;
  website: string;
  logo: string;
  extra?: { [key: string]: any } | undefined;
}

/** Proposal for any DAO type */
export interface Proposal {
  id: string;
  title: string;
  description: string;
  actionType: string;
  payload?: { [key: string]: any } | undefined;
  proposer?: Address | undefined;
  proposedAt?: Date | undefined;
  deadline?: Date | undefined;
  queuedAt?: Date | undefined;
  executableAt?: Date | undefined;
}

/** Vote record */
export interface Vote {
  voter?: Address | undefined;
  choice: VoteChoice;
  weight: number;
  votedAt?: Date | undefined;
}

/** Vote tally */
export interface VoteTally {
  votesFor: number;
  votesAgainst: number;
  votesAbstain: number;
  votes: Vote[];
}

export interface SingleOwnerDAO {
  name: string;
  owner?: Address | undefined;
  pendingOwner?: Address | undefined;
  transferInitiatedAt?: Date | undefined;
  actions: SingleOwnerAction[];
  ownershipHistory: OwnershipTransfer[];
  metadata?: DAOMetadata | undefined;
  status: DAOStatus;
}

export interface SingleOwnerAction {
  id: string;
  actionType: string;
  payload?: { [key: string]: any } | undefined;
  executedAt?: Date | undefined;
}

export interface OwnershipTransfer {
  from?: Address | undefined;
  to?: Address | undefined;
  at?: Date | undefined;
}

export interface MultisigDAO {
  name: string;
  signers: Address[];
  threshold: number;
  proposalTtlMs: number;
  proposal?:
    | Proposal
    | undefined;
  /** signer address -> timestamp */
  signatures: { [key: string]: number };
  actions: MultisigAction[];
  cancelledProposals: Proposal[];
  metadata?: DAOMetadata | undefined;
  status: DAOStatus;
}

export interface MultisigDAO_SignaturesEntry {
  key: string;
  value: number;
}

export interface MultisigAction {
  proposal?: Proposal | undefined;
  signatures: { [key: string]: number };
  executedAt?: Date | undefined;
}

export interface MultisigAction_SignaturesEntry {
  key: string;
  value: number;
}

export interface TokenDAO {
  name: string;
  tokenId: string;
  /** address -> balance */
  balances: { [key: string]: number };
  /** delegator -> delegatee */
  delegations: { [key: string]: string };
  /** Governance parameters */
  proposalThreshold: number;
  votingPeriodMs: number;
  timelockMs: number;
  quorum: number;
  /** Current state */
  proposal?: Proposal | undefined;
  votes?:
    | VoteTally
    | undefined;
  /** History */
  executedProposals: TokenProposalResult[];
  rejectedProposals: TokenProposalResult[];
  cancelledProposals: Proposal[];
  metadata?: DAOMetadata | undefined;
  status: DAOStatus;
}

export interface TokenDAO_BalancesEntry {
  key: string;
  value: number;
}

export interface TokenDAO_DelegationsEntry {
  key: string;
  value: string;
}

export interface TokenProposalResult {
  proposal?: Proposal | undefined;
  votes?: VoteTally | undefined;
  finalizedAt?: Date | undefined;
}

export interface ThresholdDAO {
  name: string;
  members: Address[];
  memberJoinedAt: { [key: string]: number };
  /** Thresholds */
  memberThreshold: number;
  voteThreshold: number;
  proposeThreshold: number;
  quorum: number;
  votingPeriodMs: number;
  /** Current state */
  proposal?: Proposal | undefined;
  votes?:
    | ThresholdVotes
    | undefined;
  /** History */
  history: ThresholdHistoryEntry[];
  metadata?: DAOMetadata | undefined;
  status: DAOStatus;
}

export interface ThresholdDAO_MemberJoinedAtEntry {
  key: string;
  value: number;
}

export interface ThresholdVotes {
  votesFor: Address[];
  votesAgainst: Address[];
  votesAbstain: Address[];
}

export interface ThresholdHistoryEntry {
  /** "executed" | "rejected" */
  resultType: string;
  proposal?: Proposal | undefined;
  votes?: ThresholdVotes | undefined;
  at?: Date | undefined;
}

export interface CreateDAORequest {
  daoType: DAOType;
  name: string;
  creator?: Address | undefined;
  metadata?:
    | DAOMetadata
    | undefined;
  /** Type-specific config (signers, thresholds, etc.) */
  config?: { [key: string]: any } | undefined;
}

export interface ProposeRequest {
  daoId: string;
  proposer?: Address | undefined;
  title: string;
  description: string;
  actionType: string;
  payload?: { [key: string]: any } | undefined;
}

export interface VoteRequest {
  daoId: string;
  voter?: Address | undefined;
  choice: VoteChoice;
}

export interface ExecuteRequest {
  daoId: string;
  executor?: Address | undefined;
}

function createBaseDAOMetadata(): DAOMetadata {
  return { description: "", website: "", logo: "", extra: undefined };
}

export const DAOMetadata: MessageFns<DAOMetadata> = {
  encode(message: DAOMetadata, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.description !== "") {
      writer.uint32(10).string(message.description);
    }
    if (message.website !== "") {
      writer.uint32(18).string(message.website);
    }
    if (message.logo !== "") {
      writer.uint32(26).string(message.logo);
    }
    if (message.extra !== undefined) {
      Struct.encode(Struct.wrap(message.extra), writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DAOMetadata {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDAOMetadata();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.description = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.website = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.logo = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.extra = Struct.unwrap(Struct.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DAOMetadata {
    return {
      description: isSet(object.description) ? globalThis.String(object.description) : "",
      website: isSet(object.website) ? globalThis.String(object.website) : "",
      logo: isSet(object.logo) ? globalThis.String(object.logo) : "",
      extra: isObject(object.extra) ? object.extra : undefined,
    };
  },

  toJSON(message: DAOMetadata): unknown {
    const obj: any = {};
    if (message.description !== "") {
      obj.description = message.description;
    }
    if (message.website !== "") {
      obj.website = message.website;
    }
    if (message.logo !== "") {
      obj.logo = message.logo;
    }
    if (message.extra !== undefined) {
      obj.extra = message.extra;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DAOMetadata>, I>>(base?: I): DAOMetadata {
    return DAOMetadata.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DAOMetadata>, I>>(object: I): DAOMetadata {
    const message = createBaseDAOMetadata();
    message.description = object.description ?? "";
    message.website = object.website ?? "";
    message.logo = object.logo ?? "";
    message.extra = object.extra ?? undefined;
    return message;
  },
};

function createBaseProposal(): Proposal {
  return {
    id: "",
    title: "",
    description: "",
    actionType: "",
    payload: undefined,
    proposer: undefined,
    proposedAt: undefined,
    deadline: undefined,
    queuedAt: undefined,
    executableAt: undefined,
  };
}

export const Proposal: MessageFns<Proposal> = {
  encode(message: Proposal, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.title !== "") {
      writer.uint32(18).string(message.title);
    }
    if (message.description !== "") {
      writer.uint32(26).string(message.description);
    }
    if (message.actionType !== "") {
      writer.uint32(34).string(message.actionType);
    }
    if (message.payload !== undefined) {
      Struct.encode(Struct.wrap(message.payload), writer.uint32(42).fork()).join();
    }
    if (message.proposer !== undefined) {
      Address.encode(message.proposer, writer.uint32(50).fork()).join();
    }
    if (message.proposedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.proposedAt), writer.uint32(58).fork()).join();
    }
    if (message.deadline !== undefined) {
      Timestamp.encode(toTimestamp(message.deadline), writer.uint32(66).fork()).join();
    }
    if (message.queuedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.queuedAt), writer.uint32(74).fork()).join();
    }
    if (message.executableAt !== undefined) {
      Timestamp.encode(toTimestamp(message.executableAt), writer.uint32(82).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Proposal {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseProposal();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.title = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.description = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.actionType = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.payload = Struct.unwrap(Struct.decode(reader, reader.uint32()));
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.proposer = Address.decode(reader, reader.uint32());
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.proposedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.deadline = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.queuedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.executableAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Proposal {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      title: isSet(object.title) ? globalThis.String(object.title) : "",
      description: isSet(object.description) ? globalThis.String(object.description) : "",
      actionType: isSet(object.actionType)
        ? globalThis.String(object.actionType)
        : isSet(object.action_type)
        ? globalThis.String(object.action_type)
        : "",
      payload: isObject(object.payload) ? object.payload : undefined,
      proposer: isSet(object.proposer) ? Address.fromJSON(object.proposer) : undefined,
      proposedAt: isSet(object.proposedAt)
        ? fromJsonTimestamp(object.proposedAt)
        : isSet(object.proposed_at)
        ? fromJsonTimestamp(object.proposed_at)
        : undefined,
      deadline: isSet(object.deadline) ? fromJsonTimestamp(object.deadline) : undefined,
      queuedAt: isSet(object.queuedAt)
        ? fromJsonTimestamp(object.queuedAt)
        : isSet(object.queued_at)
        ? fromJsonTimestamp(object.queued_at)
        : undefined,
      executableAt: isSet(object.executableAt)
        ? fromJsonTimestamp(object.executableAt)
        : isSet(object.executable_at)
        ? fromJsonTimestamp(object.executable_at)
        : undefined,
    };
  },

  toJSON(message: Proposal): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.title !== "") {
      obj.title = message.title;
    }
    if (message.description !== "") {
      obj.description = message.description;
    }
    if (message.actionType !== "") {
      obj.actionType = message.actionType;
    }
    if (message.payload !== undefined) {
      obj.payload = message.payload;
    }
    if (message.proposer !== undefined) {
      obj.proposer = Address.toJSON(message.proposer);
    }
    if (message.proposedAt !== undefined) {
      obj.proposedAt = message.proposedAt.toISOString();
    }
    if (message.deadline !== undefined) {
      obj.deadline = message.deadline.toISOString();
    }
    if (message.queuedAt !== undefined) {
      obj.queuedAt = message.queuedAt.toISOString();
    }
    if (message.executableAt !== undefined) {
      obj.executableAt = message.executableAt.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Proposal>, I>>(base?: I): Proposal {
    return Proposal.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Proposal>, I>>(object: I): Proposal {
    const message = createBaseProposal();
    message.id = object.id ?? "";
    message.title = object.title ?? "";
    message.description = object.description ?? "";
    message.actionType = object.actionType ?? "";
    message.payload = object.payload ?? undefined;
    message.proposer = (object.proposer !== undefined && object.proposer !== null)
      ? Address.fromPartial(object.proposer)
      : undefined;
    message.proposedAt = object.proposedAt ?? undefined;
    message.deadline = object.deadline ?? undefined;
    message.queuedAt = object.queuedAt ?? undefined;
    message.executableAt = object.executableAt ?? undefined;
    return message;
  },
};

function createBaseVote(): Vote {
  return { voter: undefined, choice: VoteChoice.VOTE_CHOICE_UNSPECIFIED, weight: 0, votedAt: undefined };
}

export const Vote: MessageFns<Vote> = {
  encode(message: Vote, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.voter !== undefined) {
      Address.encode(message.voter, writer.uint32(10).fork()).join();
    }
    if (message.choice !== VoteChoice.VOTE_CHOICE_UNSPECIFIED) {
      writer.uint32(16).int32(voteChoiceToNumber(message.choice));
    }
    if (message.weight !== 0) {
      writer.uint32(24).int64(message.weight);
    }
    if (message.votedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.votedAt), writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Vote {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseVote();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.voter = Address.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.choice = voteChoiceFromJSON(reader.int32());
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.weight = longToNumber(reader.int64());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.votedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Vote {
    return {
      voter: isSet(object.voter) ? Address.fromJSON(object.voter) : undefined,
      choice: isSet(object.choice) ? voteChoiceFromJSON(object.choice) : VoteChoice.VOTE_CHOICE_UNSPECIFIED,
      weight: isSet(object.weight) ? globalThis.Number(object.weight) : 0,
      votedAt: isSet(object.votedAt)
        ? fromJsonTimestamp(object.votedAt)
        : isSet(object.voted_at)
        ? fromJsonTimestamp(object.voted_at)
        : undefined,
    };
  },

  toJSON(message: Vote): unknown {
    const obj: any = {};
    if (message.voter !== undefined) {
      obj.voter = Address.toJSON(message.voter);
    }
    if (message.choice !== VoteChoice.VOTE_CHOICE_UNSPECIFIED) {
      obj.choice = voteChoiceToJSON(message.choice);
    }
    if (message.weight !== 0) {
      obj.weight = Math.round(message.weight);
    }
    if (message.votedAt !== undefined) {
      obj.votedAt = message.votedAt.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Vote>, I>>(base?: I): Vote {
    return Vote.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Vote>, I>>(object: I): Vote {
    const message = createBaseVote();
    message.voter = (object.voter !== undefined && object.voter !== null)
      ? Address.fromPartial(object.voter)
      : undefined;
    message.choice = object.choice ?? VoteChoice.VOTE_CHOICE_UNSPECIFIED;
    message.weight = object.weight ?? 0;
    message.votedAt = object.votedAt ?? undefined;
    return message;
  },
};

function createBaseVoteTally(): VoteTally {
  return { votesFor: 0, votesAgainst: 0, votesAbstain: 0, votes: [] };
}

export const VoteTally: MessageFns<VoteTally> = {
  encode(message: VoteTally, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.votesFor !== 0) {
      writer.uint32(8).int64(message.votesFor);
    }
    if (message.votesAgainst !== 0) {
      writer.uint32(16).int64(message.votesAgainst);
    }
    if (message.votesAbstain !== 0) {
      writer.uint32(24).int64(message.votesAbstain);
    }
    for (const v of message.votes) {
      Vote.encode(v!, writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): VoteTally {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseVoteTally();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.votesFor = longToNumber(reader.int64());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.votesAgainst = longToNumber(reader.int64());
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.votesAbstain = longToNumber(reader.int64());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.votes.push(Vote.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): VoteTally {
    return {
      votesFor: isSet(object.votesFor)
        ? globalThis.Number(object.votesFor)
        : isSet(object.votes_for)
        ? globalThis.Number(object.votes_for)
        : 0,
      votesAgainst: isSet(object.votesAgainst)
        ? globalThis.Number(object.votesAgainst)
        : isSet(object.votes_against)
        ? globalThis.Number(object.votes_against)
        : 0,
      votesAbstain: isSet(object.votesAbstain)
        ? globalThis.Number(object.votesAbstain)
        : isSet(object.votes_abstain)
        ? globalThis.Number(object.votes_abstain)
        : 0,
      votes: globalThis.Array.isArray(object?.votes) ? object.votes.map((e: any) => Vote.fromJSON(e)) : [],
    };
  },

  toJSON(message: VoteTally): unknown {
    const obj: any = {};
    if (message.votesFor !== 0) {
      obj.votesFor = Math.round(message.votesFor);
    }
    if (message.votesAgainst !== 0) {
      obj.votesAgainst = Math.round(message.votesAgainst);
    }
    if (message.votesAbstain !== 0) {
      obj.votesAbstain = Math.round(message.votesAbstain);
    }
    if (message.votes?.length) {
      obj.votes = message.votes.map((e) => Vote.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<VoteTally>, I>>(base?: I): VoteTally {
    return VoteTally.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<VoteTally>, I>>(object: I): VoteTally {
    const message = createBaseVoteTally();
    message.votesFor = object.votesFor ?? 0;
    message.votesAgainst = object.votesAgainst ?? 0;
    message.votesAbstain = object.votesAbstain ?? 0;
    message.votes = object.votes?.map((e) => Vote.fromPartial(e)) || [];
    return message;
  },
};

function createBaseSingleOwnerDAO(): SingleOwnerDAO {
  return {
    name: "",
    owner: undefined,
    pendingOwner: undefined,
    transferInitiatedAt: undefined,
    actions: [],
    ownershipHistory: [],
    metadata: undefined,
    status: DAOStatus.DAO_STATUS_UNSPECIFIED,
  };
}

export const SingleOwnerDAO: MessageFns<SingleOwnerDAO> = {
  encode(message: SingleOwnerDAO, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.owner !== undefined) {
      Address.encode(message.owner, writer.uint32(18).fork()).join();
    }
    if (message.pendingOwner !== undefined) {
      Address.encode(message.pendingOwner, writer.uint32(26).fork()).join();
    }
    if (message.transferInitiatedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.transferInitiatedAt), writer.uint32(34).fork()).join();
    }
    for (const v of message.actions) {
      SingleOwnerAction.encode(v!, writer.uint32(42).fork()).join();
    }
    for (const v of message.ownershipHistory) {
      OwnershipTransfer.encode(v!, writer.uint32(50).fork()).join();
    }
    if (message.metadata !== undefined) {
      DAOMetadata.encode(message.metadata, writer.uint32(58).fork()).join();
    }
    if (message.status !== DAOStatus.DAO_STATUS_UNSPECIFIED) {
      writer.uint32(64).int32(dAOStatusToNumber(message.status));
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SingleOwnerDAO {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSingleOwnerDAO();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.owner = Address.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.pendingOwner = Address.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.transferInitiatedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.actions.push(SingleOwnerAction.decode(reader, reader.uint32()));
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.ownershipHistory.push(OwnershipTransfer.decode(reader, reader.uint32()));
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.metadata = DAOMetadata.decode(reader, reader.uint32());
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.status = dAOStatusFromJSON(reader.int32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SingleOwnerDAO {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      owner: isSet(object.owner) ? Address.fromJSON(object.owner) : undefined,
      pendingOwner: isSet(object.pendingOwner)
        ? Address.fromJSON(object.pendingOwner)
        : isSet(object.pending_owner)
        ? Address.fromJSON(object.pending_owner)
        : undefined,
      transferInitiatedAt: isSet(object.transferInitiatedAt)
        ? fromJsonTimestamp(object.transferInitiatedAt)
        : isSet(object.transfer_initiated_at)
        ? fromJsonTimestamp(object.transfer_initiated_at)
        : undefined,
      actions: globalThis.Array.isArray(object?.actions)
        ? object.actions.map((e: any) => SingleOwnerAction.fromJSON(e))
        : [],
      ownershipHistory: globalThis.Array.isArray(object?.ownershipHistory)
        ? object.ownershipHistory.map((e: any) => OwnershipTransfer.fromJSON(e))
        : globalThis.Array.isArray(object?.ownership_history)
        ? object.ownership_history.map((e: any) => OwnershipTransfer.fromJSON(e))
        : [],
      metadata: isSet(object.metadata) ? DAOMetadata.fromJSON(object.metadata) : undefined,
      status: isSet(object.status) ? dAOStatusFromJSON(object.status) : DAOStatus.DAO_STATUS_UNSPECIFIED,
    };
  },

  toJSON(message: SingleOwnerDAO): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.owner !== undefined) {
      obj.owner = Address.toJSON(message.owner);
    }
    if (message.pendingOwner !== undefined) {
      obj.pendingOwner = Address.toJSON(message.pendingOwner);
    }
    if (message.transferInitiatedAt !== undefined) {
      obj.transferInitiatedAt = message.transferInitiatedAt.toISOString();
    }
    if (message.actions?.length) {
      obj.actions = message.actions.map((e) => SingleOwnerAction.toJSON(e));
    }
    if (message.ownershipHistory?.length) {
      obj.ownershipHistory = message.ownershipHistory.map((e) => OwnershipTransfer.toJSON(e));
    }
    if (message.metadata !== undefined) {
      obj.metadata = DAOMetadata.toJSON(message.metadata);
    }
    if (message.status !== DAOStatus.DAO_STATUS_UNSPECIFIED) {
      obj.status = dAOStatusToJSON(message.status);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SingleOwnerDAO>, I>>(base?: I): SingleOwnerDAO {
    return SingleOwnerDAO.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SingleOwnerDAO>, I>>(object: I): SingleOwnerDAO {
    const message = createBaseSingleOwnerDAO();
    message.name = object.name ?? "";
    message.owner = (object.owner !== undefined && object.owner !== null)
      ? Address.fromPartial(object.owner)
      : undefined;
    message.pendingOwner = (object.pendingOwner !== undefined && object.pendingOwner !== null)
      ? Address.fromPartial(object.pendingOwner)
      : undefined;
    message.transferInitiatedAt = object.transferInitiatedAt ?? undefined;
    message.actions = object.actions?.map((e) => SingleOwnerAction.fromPartial(e)) || [];
    message.ownershipHistory = object.ownershipHistory?.map((e) => OwnershipTransfer.fromPartial(e)) || [];
    message.metadata = (object.metadata !== undefined && object.metadata !== null)
      ? DAOMetadata.fromPartial(object.metadata)
      : undefined;
    message.status = object.status ?? DAOStatus.DAO_STATUS_UNSPECIFIED;
    return message;
  },
};

function createBaseSingleOwnerAction(): SingleOwnerAction {
  return { id: "", actionType: "", payload: undefined, executedAt: undefined };
}

export const SingleOwnerAction: MessageFns<SingleOwnerAction> = {
  encode(message: SingleOwnerAction, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.actionType !== "") {
      writer.uint32(18).string(message.actionType);
    }
    if (message.payload !== undefined) {
      Struct.encode(Struct.wrap(message.payload), writer.uint32(26).fork()).join();
    }
    if (message.executedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.executedAt), writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SingleOwnerAction {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSingleOwnerAction();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.actionType = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.payload = Struct.unwrap(Struct.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.executedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SingleOwnerAction {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      actionType: isSet(object.actionType)
        ? globalThis.String(object.actionType)
        : isSet(object.action_type)
        ? globalThis.String(object.action_type)
        : "",
      payload: isObject(object.payload) ? object.payload : undefined,
      executedAt: isSet(object.executedAt)
        ? fromJsonTimestamp(object.executedAt)
        : isSet(object.executed_at)
        ? fromJsonTimestamp(object.executed_at)
        : undefined,
    };
  },

  toJSON(message: SingleOwnerAction): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.actionType !== "") {
      obj.actionType = message.actionType;
    }
    if (message.payload !== undefined) {
      obj.payload = message.payload;
    }
    if (message.executedAt !== undefined) {
      obj.executedAt = message.executedAt.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SingleOwnerAction>, I>>(base?: I): SingleOwnerAction {
    return SingleOwnerAction.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SingleOwnerAction>, I>>(object: I): SingleOwnerAction {
    const message = createBaseSingleOwnerAction();
    message.id = object.id ?? "";
    message.actionType = object.actionType ?? "";
    message.payload = object.payload ?? undefined;
    message.executedAt = object.executedAt ?? undefined;
    return message;
  },
};

function createBaseOwnershipTransfer(): OwnershipTransfer {
  return { from: undefined, to: undefined, at: undefined };
}

export const OwnershipTransfer: MessageFns<OwnershipTransfer> = {
  encode(message: OwnershipTransfer, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.from !== undefined) {
      Address.encode(message.from, writer.uint32(10).fork()).join();
    }
    if (message.to !== undefined) {
      Address.encode(message.to, writer.uint32(18).fork()).join();
    }
    if (message.at !== undefined) {
      Timestamp.encode(toTimestamp(message.at), writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): OwnershipTransfer {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseOwnershipTransfer();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.from = Address.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.to = Address.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.at = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): OwnershipTransfer {
    return {
      from: isSet(object.from) ? Address.fromJSON(object.from) : undefined,
      to: isSet(object.to) ? Address.fromJSON(object.to) : undefined,
      at: isSet(object.at) ? fromJsonTimestamp(object.at) : undefined,
    };
  },

  toJSON(message: OwnershipTransfer): unknown {
    const obj: any = {};
    if (message.from !== undefined) {
      obj.from = Address.toJSON(message.from);
    }
    if (message.to !== undefined) {
      obj.to = Address.toJSON(message.to);
    }
    if (message.at !== undefined) {
      obj.at = message.at.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<OwnershipTransfer>, I>>(base?: I): OwnershipTransfer {
    return OwnershipTransfer.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<OwnershipTransfer>, I>>(object: I): OwnershipTransfer {
    const message = createBaseOwnershipTransfer();
    message.from = (object.from !== undefined && object.from !== null) ? Address.fromPartial(object.from) : undefined;
    message.to = (object.to !== undefined && object.to !== null) ? Address.fromPartial(object.to) : undefined;
    message.at = object.at ?? undefined;
    return message;
  },
};

function createBaseMultisigDAO(): MultisigDAO {
  return {
    name: "",
    signers: [],
    threshold: 0,
    proposalTtlMs: 0,
    proposal: undefined,
    signatures: {},
    actions: [],
    cancelledProposals: [],
    metadata: undefined,
    status: DAOStatus.DAO_STATUS_UNSPECIFIED,
  };
}

export const MultisigDAO: MessageFns<MultisigDAO> = {
  encode(message: MultisigDAO, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    for (const v of message.signers) {
      Address.encode(v!, writer.uint32(18).fork()).join();
    }
    if (message.threshold !== 0) {
      writer.uint32(24).int32(message.threshold);
    }
    if (message.proposalTtlMs !== 0) {
      writer.uint32(32).int64(message.proposalTtlMs);
    }
    if (message.proposal !== undefined) {
      Proposal.encode(message.proposal, writer.uint32(42).fork()).join();
    }
    globalThis.Object.entries(message.signatures).forEach(([key, value]: [string, number]) => {
      MultisigDAO_SignaturesEntry.encode({ key: key as any, value }, writer.uint32(50).fork()).join();
    });
    for (const v of message.actions) {
      MultisigAction.encode(v!, writer.uint32(58).fork()).join();
    }
    for (const v of message.cancelledProposals) {
      Proposal.encode(v!, writer.uint32(66).fork()).join();
    }
    if (message.metadata !== undefined) {
      DAOMetadata.encode(message.metadata, writer.uint32(74).fork()).join();
    }
    if (message.status !== DAOStatus.DAO_STATUS_UNSPECIFIED) {
      writer.uint32(80).int32(dAOStatusToNumber(message.status));
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MultisigDAO {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMultisigDAO();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.signers.push(Address.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.threshold = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.proposalTtlMs = longToNumber(reader.int64());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.proposal = Proposal.decode(reader, reader.uint32());
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          const entry6 = MultisigDAO_SignaturesEntry.decode(reader, reader.uint32());
          if (entry6.value !== undefined) {
            message.signatures[entry6.key] = entry6.value;
          }
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.actions.push(MultisigAction.decode(reader, reader.uint32()));
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.cancelledProposals.push(Proposal.decode(reader, reader.uint32()));
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.metadata = DAOMetadata.decode(reader, reader.uint32());
          continue;
        }
        case 10: {
          if (tag !== 80) {
            break;
          }

          message.status = dAOStatusFromJSON(reader.int32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MultisigDAO {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      signers: globalThis.Array.isArray(object?.signers) ? object.signers.map((e: any) => Address.fromJSON(e)) : [],
      threshold: isSet(object.threshold) ? globalThis.Number(object.threshold) : 0,
      proposalTtlMs: isSet(object.proposalTtlMs)
        ? globalThis.Number(object.proposalTtlMs)
        : isSet(object.proposal_ttl_ms)
        ? globalThis.Number(object.proposal_ttl_ms)
        : 0,
      proposal: isSet(object.proposal) ? Proposal.fromJSON(object.proposal) : undefined,
      signatures: isObject(object.signatures)
        ? (globalThis.Object.entries(object.signatures) as [string, any][]).reduce(
          (acc: { [key: string]: number }, [key, value]: [string, any]) => {
            acc[key] = globalThis.Number(value);
            return acc;
          },
          {},
        )
        : {},
      actions: globalThis.Array.isArray(object?.actions)
        ? object.actions.map((e: any) => MultisigAction.fromJSON(e))
        : [],
      cancelledProposals: globalThis.Array.isArray(object?.cancelledProposals)
        ? object.cancelledProposals.map((e: any) => Proposal.fromJSON(e))
        : globalThis.Array.isArray(object?.cancelled_proposals)
        ? object.cancelled_proposals.map((e: any) => Proposal.fromJSON(e))
        : [],
      metadata: isSet(object.metadata) ? DAOMetadata.fromJSON(object.metadata) : undefined,
      status: isSet(object.status) ? dAOStatusFromJSON(object.status) : DAOStatus.DAO_STATUS_UNSPECIFIED,
    };
  },

  toJSON(message: MultisigDAO): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.signers?.length) {
      obj.signers = message.signers.map((e) => Address.toJSON(e));
    }
    if (message.threshold !== 0) {
      obj.threshold = Math.round(message.threshold);
    }
    if (message.proposalTtlMs !== 0) {
      obj.proposalTtlMs = Math.round(message.proposalTtlMs);
    }
    if (message.proposal !== undefined) {
      obj.proposal = Proposal.toJSON(message.proposal);
    }
    if (message.signatures) {
      const entries = globalThis.Object.entries(message.signatures) as [string, number][];
      if (entries.length > 0) {
        obj.signatures = {};
        entries.forEach(([k, v]) => {
          obj.signatures[k] = Math.round(v);
        });
      }
    }
    if (message.actions?.length) {
      obj.actions = message.actions.map((e) => MultisigAction.toJSON(e));
    }
    if (message.cancelledProposals?.length) {
      obj.cancelledProposals = message.cancelledProposals.map((e) => Proposal.toJSON(e));
    }
    if (message.metadata !== undefined) {
      obj.metadata = DAOMetadata.toJSON(message.metadata);
    }
    if (message.status !== DAOStatus.DAO_STATUS_UNSPECIFIED) {
      obj.status = dAOStatusToJSON(message.status);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MultisigDAO>, I>>(base?: I): MultisigDAO {
    return MultisigDAO.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MultisigDAO>, I>>(object: I): MultisigDAO {
    const message = createBaseMultisigDAO();
    message.name = object.name ?? "";
    message.signers = object.signers?.map((e) => Address.fromPartial(e)) || [];
    message.threshold = object.threshold ?? 0;
    message.proposalTtlMs = object.proposalTtlMs ?? 0;
    message.proposal = (object.proposal !== undefined && object.proposal !== null)
      ? Proposal.fromPartial(object.proposal)
      : undefined;
    message.signatures = (globalThis.Object.entries(object.signatures ?? {}) as [string, number][]).reduce(
      (acc: { [key: string]: number }, [key, value]: [string, number]) => {
        if (value !== undefined) {
          acc[key] = globalThis.Number(value);
        }
        return acc;
      },
      {},
    );
    message.actions = object.actions?.map((e) => MultisigAction.fromPartial(e)) || [];
    message.cancelledProposals = object.cancelledProposals?.map((e) => Proposal.fromPartial(e)) || [];
    message.metadata = (object.metadata !== undefined && object.metadata !== null)
      ? DAOMetadata.fromPartial(object.metadata)
      : undefined;
    message.status = object.status ?? DAOStatus.DAO_STATUS_UNSPECIFIED;
    return message;
  },
};

function createBaseMultisigDAO_SignaturesEntry(): MultisigDAO_SignaturesEntry {
  return { key: "", value: 0 };
}

export const MultisigDAO_SignaturesEntry: MessageFns<MultisigDAO_SignaturesEntry> = {
  encode(message: MultisigDAO_SignaturesEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== 0) {
      writer.uint32(16).int64(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MultisigDAO_SignaturesEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMultisigDAO_SignaturesEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.value = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MultisigDAO_SignaturesEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.Number(object.value) : 0,
    };
  },

  toJSON(message: MultisigDAO_SignaturesEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== 0) {
      obj.value = Math.round(message.value);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MultisigDAO_SignaturesEntry>, I>>(base?: I): MultisigDAO_SignaturesEntry {
    return MultisigDAO_SignaturesEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MultisigDAO_SignaturesEntry>, I>>(object: I): MultisigDAO_SignaturesEntry {
    const message = createBaseMultisigDAO_SignaturesEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? 0;
    return message;
  },
};

function createBaseMultisigAction(): MultisigAction {
  return { proposal: undefined, signatures: {}, executedAt: undefined };
}

export const MultisigAction: MessageFns<MultisigAction> = {
  encode(message: MultisigAction, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.proposal !== undefined) {
      Proposal.encode(message.proposal, writer.uint32(10).fork()).join();
    }
    globalThis.Object.entries(message.signatures).forEach(([key, value]: [string, number]) => {
      MultisigAction_SignaturesEntry.encode({ key: key as any, value }, writer.uint32(18).fork()).join();
    });
    if (message.executedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.executedAt), writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MultisigAction {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMultisigAction();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.proposal = Proposal.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          const entry2 = MultisigAction_SignaturesEntry.decode(reader, reader.uint32());
          if (entry2.value !== undefined) {
            message.signatures[entry2.key] = entry2.value;
          }
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.executedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MultisigAction {
    return {
      proposal: isSet(object.proposal) ? Proposal.fromJSON(object.proposal) : undefined,
      signatures: isObject(object.signatures)
        ? (globalThis.Object.entries(object.signatures) as [string, any][]).reduce(
          (acc: { [key: string]: number }, [key, value]: [string, any]) => {
            acc[key] = globalThis.Number(value);
            return acc;
          },
          {},
        )
        : {},
      executedAt: isSet(object.executedAt)
        ? fromJsonTimestamp(object.executedAt)
        : isSet(object.executed_at)
        ? fromJsonTimestamp(object.executed_at)
        : undefined,
    };
  },

  toJSON(message: MultisigAction): unknown {
    const obj: any = {};
    if (message.proposal !== undefined) {
      obj.proposal = Proposal.toJSON(message.proposal);
    }
    if (message.signatures) {
      const entries = globalThis.Object.entries(message.signatures) as [string, number][];
      if (entries.length > 0) {
        obj.signatures = {};
        entries.forEach(([k, v]) => {
          obj.signatures[k] = Math.round(v);
        });
      }
    }
    if (message.executedAt !== undefined) {
      obj.executedAt = message.executedAt.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MultisigAction>, I>>(base?: I): MultisigAction {
    return MultisigAction.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MultisigAction>, I>>(object: I): MultisigAction {
    const message = createBaseMultisigAction();
    message.proposal = (object.proposal !== undefined && object.proposal !== null)
      ? Proposal.fromPartial(object.proposal)
      : undefined;
    message.signatures = (globalThis.Object.entries(object.signatures ?? {}) as [string, number][]).reduce(
      (acc: { [key: string]: number }, [key, value]: [string, number]) => {
        if (value !== undefined) {
          acc[key] = globalThis.Number(value);
        }
        return acc;
      },
      {},
    );
    message.executedAt = object.executedAt ?? undefined;
    return message;
  },
};

function createBaseMultisigAction_SignaturesEntry(): MultisigAction_SignaturesEntry {
  return { key: "", value: 0 };
}

export const MultisigAction_SignaturesEntry: MessageFns<MultisigAction_SignaturesEntry> = {
  encode(message: MultisigAction_SignaturesEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== 0) {
      writer.uint32(16).int64(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MultisigAction_SignaturesEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMultisigAction_SignaturesEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.value = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MultisigAction_SignaturesEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.Number(object.value) : 0,
    };
  },

  toJSON(message: MultisigAction_SignaturesEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== 0) {
      obj.value = Math.round(message.value);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MultisigAction_SignaturesEntry>, I>>(base?: I): MultisigAction_SignaturesEntry {
    return MultisigAction_SignaturesEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MultisigAction_SignaturesEntry>, I>>(
    object: I,
  ): MultisigAction_SignaturesEntry {
    const message = createBaseMultisigAction_SignaturesEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? 0;
    return message;
  },
};

function createBaseTokenDAO(): TokenDAO {
  return {
    name: "",
    tokenId: "",
    balances: {},
    delegations: {},
    proposalThreshold: 0,
    votingPeriodMs: 0,
    timelockMs: 0,
    quorum: 0,
    proposal: undefined,
    votes: undefined,
    executedProposals: [],
    rejectedProposals: [],
    cancelledProposals: [],
    metadata: undefined,
    status: DAOStatus.DAO_STATUS_UNSPECIFIED,
  };
}

export const TokenDAO: MessageFns<TokenDAO> = {
  encode(message: TokenDAO, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.tokenId !== "") {
      writer.uint32(18).string(message.tokenId);
    }
    globalThis.Object.entries(message.balances).forEach(([key, value]: [string, number]) => {
      TokenDAO_BalancesEntry.encode({ key: key as any, value }, writer.uint32(26).fork()).join();
    });
    globalThis.Object.entries(message.delegations).forEach(([key, value]: [string, string]) => {
      TokenDAO_DelegationsEntry.encode({ key: key as any, value }, writer.uint32(34).fork()).join();
    });
    if (message.proposalThreshold !== 0) {
      writer.uint32(40).int64(message.proposalThreshold);
    }
    if (message.votingPeriodMs !== 0) {
      writer.uint32(48).int64(message.votingPeriodMs);
    }
    if (message.timelockMs !== 0) {
      writer.uint32(56).int64(message.timelockMs);
    }
    if (message.quorum !== 0) {
      writer.uint32(64).int64(message.quorum);
    }
    if (message.proposal !== undefined) {
      Proposal.encode(message.proposal, writer.uint32(74).fork()).join();
    }
    if (message.votes !== undefined) {
      VoteTally.encode(message.votes, writer.uint32(82).fork()).join();
    }
    for (const v of message.executedProposals) {
      TokenProposalResult.encode(v!, writer.uint32(90).fork()).join();
    }
    for (const v of message.rejectedProposals) {
      TokenProposalResult.encode(v!, writer.uint32(98).fork()).join();
    }
    for (const v of message.cancelledProposals) {
      Proposal.encode(v!, writer.uint32(106).fork()).join();
    }
    if (message.metadata !== undefined) {
      DAOMetadata.encode(message.metadata, writer.uint32(114).fork()).join();
    }
    if (message.status !== DAOStatus.DAO_STATUS_UNSPECIFIED) {
      writer.uint32(120).int32(dAOStatusToNumber(message.status));
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TokenDAO {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTokenDAO();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.tokenId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          const entry3 = TokenDAO_BalancesEntry.decode(reader, reader.uint32());
          if (entry3.value !== undefined) {
            message.balances[entry3.key] = entry3.value;
          }
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          const entry4 = TokenDAO_DelegationsEntry.decode(reader, reader.uint32());
          if (entry4.value !== undefined) {
            message.delegations[entry4.key] = entry4.value;
          }
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.proposalThreshold = longToNumber(reader.int64());
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.votingPeriodMs = longToNumber(reader.int64());
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.timelockMs = longToNumber(reader.int64());
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.quorum = longToNumber(reader.int64());
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.proposal = Proposal.decode(reader, reader.uint32());
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.votes = VoteTally.decode(reader, reader.uint32());
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.executedProposals.push(TokenProposalResult.decode(reader, reader.uint32()));
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.rejectedProposals.push(TokenProposalResult.decode(reader, reader.uint32()));
          continue;
        }
        case 13: {
          if (tag !== 106) {
            break;
          }

          message.cancelledProposals.push(Proposal.decode(reader, reader.uint32()));
          continue;
        }
        case 14: {
          if (tag !== 114) {
            break;
          }

          message.metadata = DAOMetadata.decode(reader, reader.uint32());
          continue;
        }
        case 15: {
          if (tag !== 120) {
            break;
          }

          message.status = dAOStatusFromJSON(reader.int32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TokenDAO {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      tokenId: isSet(object.tokenId)
        ? globalThis.String(object.tokenId)
        : isSet(object.token_id)
        ? globalThis.String(object.token_id)
        : "",
      balances: isObject(object.balances)
        ? (globalThis.Object.entries(object.balances) as [string, any][]).reduce(
          (acc: { [key: string]: number }, [key, value]: [string, any]) => {
            acc[key] = globalThis.Number(value);
            return acc;
          },
          {},
        )
        : {},
      delegations: isObject(object.delegations)
        ? (globalThis.Object.entries(object.delegations) as [string, any][]).reduce(
          (acc: { [key: string]: string }, [key, value]: [string, any]) => {
            acc[key] = globalThis.String(value);
            return acc;
          },
          {},
        )
        : {},
      proposalThreshold: isSet(object.proposalThreshold)
        ? globalThis.Number(object.proposalThreshold)
        : isSet(object.proposal_threshold)
        ? globalThis.Number(object.proposal_threshold)
        : 0,
      votingPeriodMs: isSet(object.votingPeriodMs)
        ? globalThis.Number(object.votingPeriodMs)
        : isSet(object.voting_period_ms)
        ? globalThis.Number(object.voting_period_ms)
        : 0,
      timelockMs: isSet(object.timelockMs)
        ? globalThis.Number(object.timelockMs)
        : isSet(object.timelock_ms)
        ? globalThis.Number(object.timelock_ms)
        : 0,
      quorum: isSet(object.quorum) ? globalThis.Number(object.quorum) : 0,
      proposal: isSet(object.proposal) ? Proposal.fromJSON(object.proposal) : undefined,
      votes: isSet(object.votes) ? VoteTally.fromJSON(object.votes) : undefined,
      executedProposals: globalThis.Array.isArray(object?.executedProposals)
        ? object.executedProposals.map((e: any) => TokenProposalResult.fromJSON(e))
        : globalThis.Array.isArray(object?.executed_proposals)
        ? object.executed_proposals.map((e: any) => TokenProposalResult.fromJSON(e))
        : [],
      rejectedProposals: globalThis.Array.isArray(object?.rejectedProposals)
        ? object.rejectedProposals.map((e: any) => TokenProposalResult.fromJSON(e))
        : globalThis.Array.isArray(object?.rejected_proposals)
        ? object.rejected_proposals.map((e: any) => TokenProposalResult.fromJSON(e))
        : [],
      cancelledProposals: globalThis.Array.isArray(object?.cancelledProposals)
        ? object.cancelledProposals.map((e: any) => Proposal.fromJSON(e))
        : globalThis.Array.isArray(object?.cancelled_proposals)
        ? object.cancelled_proposals.map((e: any) => Proposal.fromJSON(e))
        : [],
      metadata: isSet(object.metadata) ? DAOMetadata.fromJSON(object.metadata) : undefined,
      status: isSet(object.status) ? dAOStatusFromJSON(object.status) : DAOStatus.DAO_STATUS_UNSPECIFIED,
    };
  },

  toJSON(message: TokenDAO): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.tokenId !== "") {
      obj.tokenId = message.tokenId;
    }
    if (message.balances) {
      const entries = globalThis.Object.entries(message.balances) as [string, number][];
      if (entries.length > 0) {
        obj.balances = {};
        entries.forEach(([k, v]) => {
          obj.balances[k] = Math.round(v);
        });
      }
    }
    if (message.delegations) {
      const entries = globalThis.Object.entries(message.delegations) as [string, string][];
      if (entries.length > 0) {
        obj.delegations = {};
        entries.forEach(([k, v]) => {
          obj.delegations[k] = v;
        });
      }
    }
    if (message.proposalThreshold !== 0) {
      obj.proposalThreshold = Math.round(message.proposalThreshold);
    }
    if (message.votingPeriodMs !== 0) {
      obj.votingPeriodMs = Math.round(message.votingPeriodMs);
    }
    if (message.timelockMs !== 0) {
      obj.timelockMs = Math.round(message.timelockMs);
    }
    if (message.quorum !== 0) {
      obj.quorum = Math.round(message.quorum);
    }
    if (message.proposal !== undefined) {
      obj.proposal = Proposal.toJSON(message.proposal);
    }
    if (message.votes !== undefined) {
      obj.votes = VoteTally.toJSON(message.votes);
    }
    if (message.executedProposals?.length) {
      obj.executedProposals = message.executedProposals.map((e) => TokenProposalResult.toJSON(e));
    }
    if (message.rejectedProposals?.length) {
      obj.rejectedProposals = message.rejectedProposals.map((e) => TokenProposalResult.toJSON(e));
    }
    if (message.cancelledProposals?.length) {
      obj.cancelledProposals = message.cancelledProposals.map((e) => Proposal.toJSON(e));
    }
    if (message.metadata !== undefined) {
      obj.metadata = DAOMetadata.toJSON(message.metadata);
    }
    if (message.status !== DAOStatus.DAO_STATUS_UNSPECIFIED) {
      obj.status = dAOStatusToJSON(message.status);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TokenDAO>, I>>(base?: I): TokenDAO {
    return TokenDAO.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TokenDAO>, I>>(object: I): TokenDAO {
    const message = createBaseTokenDAO();
    message.name = object.name ?? "";
    message.tokenId = object.tokenId ?? "";
    message.balances = (globalThis.Object.entries(object.balances ?? {}) as [string, number][]).reduce(
      (acc: { [key: string]: number }, [key, value]: [string, number]) => {
        if (value !== undefined) {
          acc[key] = globalThis.Number(value);
        }
        return acc;
      },
      {},
    );
    message.delegations = (globalThis.Object.entries(object.delegations ?? {}) as [string, string][]).reduce(
      (acc: { [key: string]: string }, [key, value]: [string, string]) => {
        if (value !== undefined) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      },
      {},
    );
    message.proposalThreshold = object.proposalThreshold ?? 0;
    message.votingPeriodMs = object.votingPeriodMs ?? 0;
    message.timelockMs = object.timelockMs ?? 0;
    message.quorum = object.quorum ?? 0;
    message.proposal = (object.proposal !== undefined && object.proposal !== null)
      ? Proposal.fromPartial(object.proposal)
      : undefined;
    message.votes = (object.votes !== undefined && object.votes !== null)
      ? VoteTally.fromPartial(object.votes)
      : undefined;
    message.executedProposals = object.executedProposals?.map((e) => TokenProposalResult.fromPartial(e)) || [];
    message.rejectedProposals = object.rejectedProposals?.map((e) => TokenProposalResult.fromPartial(e)) || [];
    message.cancelledProposals = object.cancelledProposals?.map((e) => Proposal.fromPartial(e)) || [];
    message.metadata = (object.metadata !== undefined && object.metadata !== null)
      ? DAOMetadata.fromPartial(object.metadata)
      : undefined;
    message.status = object.status ?? DAOStatus.DAO_STATUS_UNSPECIFIED;
    return message;
  },
};

function createBaseTokenDAO_BalancesEntry(): TokenDAO_BalancesEntry {
  return { key: "", value: 0 };
}

export const TokenDAO_BalancesEntry: MessageFns<TokenDAO_BalancesEntry> = {
  encode(message: TokenDAO_BalancesEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== 0) {
      writer.uint32(16).int64(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TokenDAO_BalancesEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTokenDAO_BalancesEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.value = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TokenDAO_BalancesEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.Number(object.value) : 0,
    };
  },

  toJSON(message: TokenDAO_BalancesEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== 0) {
      obj.value = Math.round(message.value);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TokenDAO_BalancesEntry>, I>>(base?: I): TokenDAO_BalancesEntry {
    return TokenDAO_BalancesEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TokenDAO_BalancesEntry>, I>>(object: I): TokenDAO_BalancesEntry {
    const message = createBaseTokenDAO_BalancesEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? 0;
    return message;
  },
};

function createBaseTokenDAO_DelegationsEntry(): TokenDAO_DelegationsEntry {
  return { key: "", value: "" };
}

export const TokenDAO_DelegationsEntry: MessageFns<TokenDAO_DelegationsEntry> = {
  encode(message: TokenDAO_DelegationsEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TokenDAO_DelegationsEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTokenDAO_DelegationsEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TokenDAO_DelegationsEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: TokenDAO_DelegationsEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TokenDAO_DelegationsEntry>, I>>(base?: I): TokenDAO_DelegationsEntry {
    return TokenDAO_DelegationsEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TokenDAO_DelegationsEntry>, I>>(object: I): TokenDAO_DelegationsEntry {
    const message = createBaseTokenDAO_DelegationsEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseTokenProposalResult(): TokenProposalResult {
  return { proposal: undefined, votes: undefined, finalizedAt: undefined };
}

export const TokenProposalResult: MessageFns<TokenProposalResult> = {
  encode(message: TokenProposalResult, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.proposal !== undefined) {
      Proposal.encode(message.proposal, writer.uint32(10).fork()).join();
    }
    if (message.votes !== undefined) {
      VoteTally.encode(message.votes, writer.uint32(18).fork()).join();
    }
    if (message.finalizedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.finalizedAt), writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TokenProposalResult {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTokenProposalResult();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.proposal = Proposal.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.votes = VoteTally.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.finalizedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TokenProposalResult {
    return {
      proposal: isSet(object.proposal) ? Proposal.fromJSON(object.proposal) : undefined,
      votes: isSet(object.votes) ? VoteTally.fromJSON(object.votes) : undefined,
      finalizedAt: isSet(object.finalizedAt)
        ? fromJsonTimestamp(object.finalizedAt)
        : isSet(object.finalized_at)
        ? fromJsonTimestamp(object.finalized_at)
        : undefined,
    };
  },

  toJSON(message: TokenProposalResult): unknown {
    const obj: any = {};
    if (message.proposal !== undefined) {
      obj.proposal = Proposal.toJSON(message.proposal);
    }
    if (message.votes !== undefined) {
      obj.votes = VoteTally.toJSON(message.votes);
    }
    if (message.finalizedAt !== undefined) {
      obj.finalizedAt = message.finalizedAt.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TokenProposalResult>, I>>(base?: I): TokenProposalResult {
    return TokenProposalResult.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TokenProposalResult>, I>>(object: I): TokenProposalResult {
    const message = createBaseTokenProposalResult();
    message.proposal = (object.proposal !== undefined && object.proposal !== null)
      ? Proposal.fromPartial(object.proposal)
      : undefined;
    message.votes = (object.votes !== undefined && object.votes !== null)
      ? VoteTally.fromPartial(object.votes)
      : undefined;
    message.finalizedAt = object.finalizedAt ?? undefined;
    return message;
  },
};

function createBaseThresholdDAO(): ThresholdDAO {
  return {
    name: "",
    members: [],
    memberJoinedAt: {},
    memberThreshold: 0,
    voteThreshold: 0,
    proposeThreshold: 0,
    quorum: 0,
    votingPeriodMs: 0,
    proposal: undefined,
    votes: undefined,
    history: [],
    metadata: undefined,
    status: DAOStatus.DAO_STATUS_UNSPECIFIED,
  };
}

export const ThresholdDAO: MessageFns<ThresholdDAO> = {
  encode(message: ThresholdDAO, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    for (const v of message.members) {
      Address.encode(v!, writer.uint32(18).fork()).join();
    }
    globalThis.Object.entries(message.memberJoinedAt).forEach(([key, value]: [string, number]) => {
      ThresholdDAO_MemberJoinedAtEntry.encode({ key: key as any, value }, writer.uint32(26).fork()).join();
    });
    if (message.memberThreshold !== 0) {
      writer.uint32(32).int64(message.memberThreshold);
    }
    if (message.voteThreshold !== 0) {
      writer.uint32(40).int64(message.voteThreshold);
    }
    if (message.proposeThreshold !== 0) {
      writer.uint32(48).int64(message.proposeThreshold);
    }
    if (message.quorum !== 0) {
      writer.uint32(56).int64(message.quorum);
    }
    if (message.votingPeriodMs !== 0) {
      writer.uint32(64).int64(message.votingPeriodMs);
    }
    if (message.proposal !== undefined) {
      Proposal.encode(message.proposal, writer.uint32(74).fork()).join();
    }
    if (message.votes !== undefined) {
      ThresholdVotes.encode(message.votes, writer.uint32(82).fork()).join();
    }
    for (const v of message.history) {
      ThresholdHistoryEntry.encode(v!, writer.uint32(90).fork()).join();
    }
    if (message.metadata !== undefined) {
      DAOMetadata.encode(message.metadata, writer.uint32(98).fork()).join();
    }
    if (message.status !== DAOStatus.DAO_STATUS_UNSPECIFIED) {
      writer.uint32(104).int32(dAOStatusToNumber(message.status));
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ThresholdDAO {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseThresholdDAO();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.members.push(Address.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          const entry3 = ThresholdDAO_MemberJoinedAtEntry.decode(reader, reader.uint32());
          if (entry3.value !== undefined) {
            message.memberJoinedAt[entry3.key] = entry3.value;
          }
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.memberThreshold = longToNumber(reader.int64());
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.voteThreshold = longToNumber(reader.int64());
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.proposeThreshold = longToNumber(reader.int64());
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.quorum = longToNumber(reader.int64());
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.votingPeriodMs = longToNumber(reader.int64());
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.proposal = Proposal.decode(reader, reader.uint32());
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.votes = ThresholdVotes.decode(reader, reader.uint32());
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.history.push(ThresholdHistoryEntry.decode(reader, reader.uint32()));
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.metadata = DAOMetadata.decode(reader, reader.uint32());
          continue;
        }
        case 13: {
          if (tag !== 104) {
            break;
          }

          message.status = dAOStatusFromJSON(reader.int32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ThresholdDAO {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      members: globalThis.Array.isArray(object?.members) ? object.members.map((e: any) => Address.fromJSON(e)) : [],
      memberJoinedAt: isObject(object.memberJoinedAt)
        ? (globalThis.Object.entries(object.memberJoinedAt) as [string, any][]).reduce(
          (acc: { [key: string]: number }, [key, value]: [string, any]) => {
            acc[key] = globalThis.Number(value);
            return acc;
          },
          {},
        )
        : isObject(object.member_joined_at)
        ? (globalThis.Object.entries(object.member_joined_at) as [string, any][]).reduce(
          (acc: { [key: string]: number }, [key, value]: [string, any]) => {
            acc[key] = globalThis.Number(value);
            return acc;
          },
          {},
        )
        : {},
      memberThreshold: isSet(object.memberThreshold)
        ? globalThis.Number(object.memberThreshold)
        : isSet(object.member_threshold)
        ? globalThis.Number(object.member_threshold)
        : 0,
      voteThreshold: isSet(object.voteThreshold)
        ? globalThis.Number(object.voteThreshold)
        : isSet(object.vote_threshold)
        ? globalThis.Number(object.vote_threshold)
        : 0,
      proposeThreshold: isSet(object.proposeThreshold)
        ? globalThis.Number(object.proposeThreshold)
        : isSet(object.propose_threshold)
        ? globalThis.Number(object.propose_threshold)
        : 0,
      quorum: isSet(object.quorum) ? globalThis.Number(object.quorum) : 0,
      votingPeriodMs: isSet(object.votingPeriodMs)
        ? globalThis.Number(object.votingPeriodMs)
        : isSet(object.voting_period_ms)
        ? globalThis.Number(object.voting_period_ms)
        : 0,
      proposal: isSet(object.proposal) ? Proposal.fromJSON(object.proposal) : undefined,
      votes: isSet(object.votes) ? ThresholdVotes.fromJSON(object.votes) : undefined,
      history: globalThis.Array.isArray(object?.history)
        ? object.history.map((e: any) => ThresholdHistoryEntry.fromJSON(e))
        : [],
      metadata: isSet(object.metadata) ? DAOMetadata.fromJSON(object.metadata) : undefined,
      status: isSet(object.status) ? dAOStatusFromJSON(object.status) : DAOStatus.DAO_STATUS_UNSPECIFIED,
    };
  },

  toJSON(message: ThresholdDAO): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.members?.length) {
      obj.members = message.members.map((e) => Address.toJSON(e));
    }
    if (message.memberJoinedAt) {
      const entries = globalThis.Object.entries(message.memberJoinedAt) as [string, number][];
      if (entries.length > 0) {
        obj.memberJoinedAt = {};
        entries.forEach(([k, v]) => {
          obj.memberJoinedAt[k] = Math.round(v);
        });
      }
    }
    if (message.memberThreshold !== 0) {
      obj.memberThreshold = Math.round(message.memberThreshold);
    }
    if (message.voteThreshold !== 0) {
      obj.voteThreshold = Math.round(message.voteThreshold);
    }
    if (message.proposeThreshold !== 0) {
      obj.proposeThreshold = Math.round(message.proposeThreshold);
    }
    if (message.quorum !== 0) {
      obj.quorum = Math.round(message.quorum);
    }
    if (message.votingPeriodMs !== 0) {
      obj.votingPeriodMs = Math.round(message.votingPeriodMs);
    }
    if (message.proposal !== undefined) {
      obj.proposal = Proposal.toJSON(message.proposal);
    }
    if (message.votes !== undefined) {
      obj.votes = ThresholdVotes.toJSON(message.votes);
    }
    if (message.history?.length) {
      obj.history = message.history.map((e) => ThresholdHistoryEntry.toJSON(e));
    }
    if (message.metadata !== undefined) {
      obj.metadata = DAOMetadata.toJSON(message.metadata);
    }
    if (message.status !== DAOStatus.DAO_STATUS_UNSPECIFIED) {
      obj.status = dAOStatusToJSON(message.status);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ThresholdDAO>, I>>(base?: I): ThresholdDAO {
    return ThresholdDAO.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ThresholdDAO>, I>>(object: I): ThresholdDAO {
    const message = createBaseThresholdDAO();
    message.name = object.name ?? "";
    message.members = object.members?.map((e) => Address.fromPartial(e)) || [];
    message.memberJoinedAt = (globalThis.Object.entries(object.memberJoinedAt ?? {}) as [string, number][]).reduce(
      (acc: { [key: string]: number }, [key, value]: [string, number]) => {
        if (value !== undefined) {
          acc[key] = globalThis.Number(value);
        }
        return acc;
      },
      {},
    );
    message.memberThreshold = object.memberThreshold ?? 0;
    message.voteThreshold = object.voteThreshold ?? 0;
    message.proposeThreshold = object.proposeThreshold ?? 0;
    message.quorum = object.quorum ?? 0;
    message.votingPeriodMs = object.votingPeriodMs ?? 0;
    message.proposal = (object.proposal !== undefined && object.proposal !== null)
      ? Proposal.fromPartial(object.proposal)
      : undefined;
    message.votes = (object.votes !== undefined && object.votes !== null)
      ? ThresholdVotes.fromPartial(object.votes)
      : undefined;
    message.history = object.history?.map((e) => ThresholdHistoryEntry.fromPartial(e)) || [];
    message.metadata = (object.metadata !== undefined && object.metadata !== null)
      ? DAOMetadata.fromPartial(object.metadata)
      : undefined;
    message.status = object.status ?? DAOStatus.DAO_STATUS_UNSPECIFIED;
    return message;
  },
};

function createBaseThresholdDAO_MemberJoinedAtEntry(): ThresholdDAO_MemberJoinedAtEntry {
  return { key: "", value: 0 };
}

export const ThresholdDAO_MemberJoinedAtEntry: MessageFns<ThresholdDAO_MemberJoinedAtEntry> = {
  encode(message: ThresholdDAO_MemberJoinedAtEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== 0) {
      writer.uint32(16).int64(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ThresholdDAO_MemberJoinedAtEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseThresholdDAO_MemberJoinedAtEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.value = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ThresholdDAO_MemberJoinedAtEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.Number(object.value) : 0,
    };
  },

  toJSON(message: ThresholdDAO_MemberJoinedAtEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== 0) {
      obj.value = Math.round(message.value);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ThresholdDAO_MemberJoinedAtEntry>, I>>(
    base?: I,
  ): ThresholdDAO_MemberJoinedAtEntry {
    return ThresholdDAO_MemberJoinedAtEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ThresholdDAO_MemberJoinedAtEntry>, I>>(
    object: I,
  ): ThresholdDAO_MemberJoinedAtEntry {
    const message = createBaseThresholdDAO_MemberJoinedAtEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? 0;
    return message;
  },
};

function createBaseThresholdVotes(): ThresholdVotes {
  return { votesFor: [], votesAgainst: [], votesAbstain: [] };
}

export const ThresholdVotes: MessageFns<ThresholdVotes> = {
  encode(message: ThresholdVotes, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.votesFor) {
      Address.encode(v!, writer.uint32(10).fork()).join();
    }
    for (const v of message.votesAgainst) {
      Address.encode(v!, writer.uint32(18).fork()).join();
    }
    for (const v of message.votesAbstain) {
      Address.encode(v!, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ThresholdVotes {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseThresholdVotes();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.votesFor.push(Address.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.votesAgainst.push(Address.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.votesAbstain.push(Address.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ThresholdVotes {
    return {
      votesFor: globalThis.Array.isArray(object?.votesFor)
        ? object.votesFor.map((e: any) => Address.fromJSON(e))
        : globalThis.Array.isArray(object?.votes_for)
        ? object.votes_for.map((e: any) => Address.fromJSON(e))
        : [],
      votesAgainst: globalThis.Array.isArray(object?.votesAgainst)
        ? object.votesAgainst.map((e: any) => Address.fromJSON(e))
        : globalThis.Array.isArray(object?.votes_against)
        ? object.votes_against.map((e: any) => Address.fromJSON(e))
        : [],
      votesAbstain: globalThis.Array.isArray(object?.votesAbstain)
        ? object.votesAbstain.map((e: any) => Address.fromJSON(e))
        : globalThis.Array.isArray(object?.votes_abstain)
        ? object.votes_abstain.map((e: any) => Address.fromJSON(e))
        : [],
    };
  },

  toJSON(message: ThresholdVotes): unknown {
    const obj: any = {};
    if (message.votesFor?.length) {
      obj.votesFor = message.votesFor.map((e) => Address.toJSON(e));
    }
    if (message.votesAgainst?.length) {
      obj.votesAgainst = message.votesAgainst.map((e) => Address.toJSON(e));
    }
    if (message.votesAbstain?.length) {
      obj.votesAbstain = message.votesAbstain.map((e) => Address.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ThresholdVotes>, I>>(base?: I): ThresholdVotes {
    return ThresholdVotes.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ThresholdVotes>, I>>(object: I): ThresholdVotes {
    const message = createBaseThresholdVotes();
    message.votesFor = object.votesFor?.map((e) => Address.fromPartial(e)) || [];
    message.votesAgainst = object.votesAgainst?.map((e) => Address.fromPartial(e)) || [];
    message.votesAbstain = object.votesAbstain?.map((e) => Address.fromPartial(e)) || [];
    return message;
  },
};

function createBaseThresholdHistoryEntry(): ThresholdHistoryEntry {
  return { resultType: "", proposal: undefined, votes: undefined, at: undefined };
}

export const ThresholdHistoryEntry: MessageFns<ThresholdHistoryEntry> = {
  encode(message: ThresholdHistoryEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.resultType !== "") {
      writer.uint32(10).string(message.resultType);
    }
    if (message.proposal !== undefined) {
      Proposal.encode(message.proposal, writer.uint32(18).fork()).join();
    }
    if (message.votes !== undefined) {
      ThresholdVotes.encode(message.votes, writer.uint32(26).fork()).join();
    }
    if (message.at !== undefined) {
      Timestamp.encode(toTimestamp(message.at), writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ThresholdHistoryEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseThresholdHistoryEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.resultType = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.proposal = Proposal.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.votes = ThresholdVotes.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.at = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ThresholdHistoryEntry {
    return {
      resultType: isSet(object.resultType)
        ? globalThis.String(object.resultType)
        : isSet(object.result_type)
        ? globalThis.String(object.result_type)
        : "",
      proposal: isSet(object.proposal) ? Proposal.fromJSON(object.proposal) : undefined,
      votes: isSet(object.votes) ? ThresholdVotes.fromJSON(object.votes) : undefined,
      at: isSet(object.at) ? fromJsonTimestamp(object.at) : undefined,
    };
  },

  toJSON(message: ThresholdHistoryEntry): unknown {
    const obj: any = {};
    if (message.resultType !== "") {
      obj.resultType = message.resultType;
    }
    if (message.proposal !== undefined) {
      obj.proposal = Proposal.toJSON(message.proposal);
    }
    if (message.votes !== undefined) {
      obj.votes = ThresholdVotes.toJSON(message.votes);
    }
    if (message.at !== undefined) {
      obj.at = message.at.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ThresholdHistoryEntry>, I>>(base?: I): ThresholdHistoryEntry {
    return ThresholdHistoryEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ThresholdHistoryEntry>, I>>(object: I): ThresholdHistoryEntry {
    const message = createBaseThresholdHistoryEntry();
    message.resultType = object.resultType ?? "";
    message.proposal = (object.proposal !== undefined && object.proposal !== null)
      ? Proposal.fromPartial(object.proposal)
      : undefined;
    message.votes = (object.votes !== undefined && object.votes !== null)
      ? ThresholdVotes.fromPartial(object.votes)
      : undefined;
    message.at = object.at ?? undefined;
    return message;
  },
};

function createBaseCreateDAORequest(): CreateDAORequest {
  return {
    daoType: DAOType.DAO_TYPE_UNSPECIFIED,
    name: "",
    creator: undefined,
    metadata: undefined,
    config: undefined,
  };
}

export const CreateDAORequest: MessageFns<CreateDAORequest> = {
  encode(message: CreateDAORequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.daoType !== DAOType.DAO_TYPE_UNSPECIFIED) {
      writer.uint32(8).int32(dAOTypeToNumber(message.daoType));
    }
    if (message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    if (message.creator !== undefined) {
      Address.encode(message.creator, writer.uint32(26).fork()).join();
    }
    if (message.metadata !== undefined) {
      DAOMetadata.encode(message.metadata, writer.uint32(34).fork()).join();
    }
    if (message.config !== undefined) {
      Struct.encode(Struct.wrap(message.config), writer.uint32(42).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CreateDAORequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreateDAORequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.daoType = dAOTypeFromJSON(reader.int32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.creator = Address.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.metadata = DAOMetadata.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.config = Struct.unwrap(Struct.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CreateDAORequest {
    return {
      daoType: isSet(object.daoType)
        ? dAOTypeFromJSON(object.daoType)
        : isSet(object.dao_type)
        ? dAOTypeFromJSON(object.dao_type)
        : DAOType.DAO_TYPE_UNSPECIFIED,
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      creator: isSet(object.creator) ? Address.fromJSON(object.creator) : undefined,
      metadata: isSet(object.metadata) ? DAOMetadata.fromJSON(object.metadata) : undefined,
      config: isObject(object.config) ? object.config : undefined,
    };
  },

  toJSON(message: CreateDAORequest): unknown {
    const obj: any = {};
    if (message.daoType !== DAOType.DAO_TYPE_UNSPECIFIED) {
      obj.daoType = dAOTypeToJSON(message.daoType);
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.creator !== undefined) {
      obj.creator = Address.toJSON(message.creator);
    }
    if (message.metadata !== undefined) {
      obj.metadata = DAOMetadata.toJSON(message.metadata);
    }
    if (message.config !== undefined) {
      obj.config = message.config;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CreateDAORequest>, I>>(base?: I): CreateDAORequest {
    return CreateDAORequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CreateDAORequest>, I>>(object: I): CreateDAORequest {
    const message = createBaseCreateDAORequest();
    message.daoType = object.daoType ?? DAOType.DAO_TYPE_UNSPECIFIED;
    message.name = object.name ?? "";
    message.creator = (object.creator !== undefined && object.creator !== null)
      ? Address.fromPartial(object.creator)
      : undefined;
    message.metadata = (object.metadata !== undefined && object.metadata !== null)
      ? DAOMetadata.fromPartial(object.metadata)
      : undefined;
    message.config = object.config ?? undefined;
    return message;
  },
};

function createBaseProposeRequest(): ProposeRequest {
  return { daoId: "", proposer: undefined, title: "", description: "", actionType: "", payload: undefined };
}

export const ProposeRequest: MessageFns<ProposeRequest> = {
  encode(message: ProposeRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.daoId !== "") {
      writer.uint32(10).string(message.daoId);
    }
    if (message.proposer !== undefined) {
      Address.encode(message.proposer, writer.uint32(18).fork()).join();
    }
    if (message.title !== "") {
      writer.uint32(26).string(message.title);
    }
    if (message.description !== "") {
      writer.uint32(34).string(message.description);
    }
    if (message.actionType !== "") {
      writer.uint32(42).string(message.actionType);
    }
    if (message.payload !== undefined) {
      Struct.encode(Struct.wrap(message.payload), writer.uint32(50).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ProposeRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseProposeRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.daoId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.proposer = Address.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.title = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.description = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.actionType = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.payload = Struct.unwrap(Struct.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ProposeRequest {
    return {
      daoId: isSet(object.daoId)
        ? globalThis.String(object.daoId)
        : isSet(object.dao_id)
        ? globalThis.String(object.dao_id)
        : "",
      proposer: isSet(object.proposer) ? Address.fromJSON(object.proposer) : undefined,
      title: isSet(object.title) ? globalThis.String(object.title) : "",
      description: isSet(object.description) ? globalThis.String(object.description) : "",
      actionType: isSet(object.actionType)
        ? globalThis.String(object.actionType)
        : isSet(object.action_type)
        ? globalThis.String(object.action_type)
        : "",
      payload: isObject(object.payload) ? object.payload : undefined,
    };
  },

  toJSON(message: ProposeRequest): unknown {
    const obj: any = {};
    if (message.daoId !== "") {
      obj.daoId = message.daoId;
    }
    if (message.proposer !== undefined) {
      obj.proposer = Address.toJSON(message.proposer);
    }
    if (message.title !== "") {
      obj.title = message.title;
    }
    if (message.description !== "") {
      obj.description = message.description;
    }
    if (message.actionType !== "") {
      obj.actionType = message.actionType;
    }
    if (message.payload !== undefined) {
      obj.payload = message.payload;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ProposeRequest>, I>>(base?: I): ProposeRequest {
    return ProposeRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ProposeRequest>, I>>(object: I): ProposeRequest {
    const message = createBaseProposeRequest();
    message.daoId = object.daoId ?? "";
    message.proposer = (object.proposer !== undefined && object.proposer !== null)
      ? Address.fromPartial(object.proposer)
      : undefined;
    message.title = object.title ?? "";
    message.description = object.description ?? "";
    message.actionType = object.actionType ?? "";
    message.payload = object.payload ?? undefined;
    return message;
  },
};

function createBaseVoteRequest(): VoteRequest {
  return { daoId: "", voter: undefined, choice: VoteChoice.VOTE_CHOICE_UNSPECIFIED };
}

export const VoteRequest: MessageFns<VoteRequest> = {
  encode(message: VoteRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.daoId !== "") {
      writer.uint32(10).string(message.daoId);
    }
    if (message.voter !== undefined) {
      Address.encode(message.voter, writer.uint32(18).fork()).join();
    }
    if (message.choice !== VoteChoice.VOTE_CHOICE_UNSPECIFIED) {
      writer.uint32(24).int32(voteChoiceToNumber(message.choice));
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): VoteRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseVoteRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.daoId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.voter = Address.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.choice = voteChoiceFromJSON(reader.int32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): VoteRequest {
    return {
      daoId: isSet(object.daoId)
        ? globalThis.String(object.daoId)
        : isSet(object.dao_id)
        ? globalThis.String(object.dao_id)
        : "",
      voter: isSet(object.voter) ? Address.fromJSON(object.voter) : undefined,
      choice: isSet(object.choice) ? voteChoiceFromJSON(object.choice) : VoteChoice.VOTE_CHOICE_UNSPECIFIED,
    };
  },

  toJSON(message: VoteRequest): unknown {
    const obj: any = {};
    if (message.daoId !== "") {
      obj.daoId = message.daoId;
    }
    if (message.voter !== undefined) {
      obj.voter = Address.toJSON(message.voter);
    }
    if (message.choice !== VoteChoice.VOTE_CHOICE_UNSPECIFIED) {
      obj.choice = voteChoiceToJSON(message.choice);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<VoteRequest>, I>>(base?: I): VoteRequest {
    return VoteRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<VoteRequest>, I>>(object: I): VoteRequest {
    const message = createBaseVoteRequest();
    message.daoId = object.daoId ?? "";
    message.voter = (object.voter !== undefined && object.voter !== null)
      ? Address.fromPartial(object.voter)
      : undefined;
    message.choice = object.choice ?? VoteChoice.VOTE_CHOICE_UNSPECIFIED;
    return message;
  },
};

function createBaseExecuteRequest(): ExecuteRequest {
  return { daoId: "", executor: undefined };
}

export const ExecuteRequest: MessageFns<ExecuteRequest> = {
  encode(message: ExecuteRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.daoId !== "") {
      writer.uint32(10).string(message.daoId);
    }
    if (message.executor !== undefined) {
      Address.encode(message.executor, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ExecuteRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseExecuteRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.daoId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.executor = Address.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ExecuteRequest {
    return {
      daoId: isSet(object.daoId)
        ? globalThis.String(object.daoId)
        : isSet(object.dao_id)
        ? globalThis.String(object.dao_id)
        : "",
      executor: isSet(object.executor) ? Address.fromJSON(object.executor) : undefined,
    };
  },

  toJSON(message: ExecuteRequest): unknown {
    const obj: any = {};
    if (message.daoId !== "") {
      obj.daoId = message.daoId;
    }
    if (message.executor !== undefined) {
      obj.executor = Address.toJSON(message.executor);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ExecuteRequest>, I>>(base?: I): ExecuteRequest {
    return ExecuteRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ExecuteRequest>, I>>(object: I): ExecuteRequest {
    const message = createBaseExecuteRequest();
    message.daoId = object.daoId ?? "";
    message.executor = (object.executor !== undefined && object.executor !== null)
      ? Address.fromPartial(object.executor)
      : undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}

// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.2
//   protoc               unknown
// source: ottochain/v1/delegation.proto
/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Struct, Value } from "../../google/protobuf/struct.js";
import { Timestamp } from "../../google/protobuf/timestamp.js";
export const protobufPackage = "ottochain.v1";
/** Delegation approach selector */
export var DelegationApproach;
(function (DelegationApproach) {
    DelegationApproach["DELEGATION_APPROACH_UNSPECIFIED"] = "DELEGATION_APPROACH_UNSPECIFIED";
    /** DELEGATION_APPROACH_SESSION_KEY - Temporary signing authority */
    DelegationApproach["DELEGATION_APPROACH_SESSION_KEY"] = "DELEGATION_APPROACH_SESSION_KEY";
    /** DELEGATION_APPROACH_SIGNED_INTENT - Pre-signed transaction intents */
    DelegationApproach["DELEGATION_APPROACH_SIGNED_INTENT"] = "DELEGATION_APPROACH_SIGNED_INTENT";
    DelegationApproach["UNRECOGNIZED"] = "UNRECOGNIZED";
})(DelegationApproach || (DelegationApproach = {}));
export function delegationApproachFromJSON(object) {
    switch (object) {
        case 0:
        case "DELEGATION_APPROACH_UNSPECIFIED":
            return DelegationApproach.DELEGATION_APPROACH_UNSPECIFIED;
        case 1:
        case "DELEGATION_APPROACH_SESSION_KEY":
            return DelegationApproach.DELEGATION_APPROACH_SESSION_KEY;
        case 2:
        case "DELEGATION_APPROACH_SIGNED_INTENT":
            return DelegationApproach.DELEGATION_APPROACH_SIGNED_INTENT;
        case -1:
        case "UNRECOGNIZED":
        default:
            return DelegationApproach.UNRECOGNIZED;
    }
}
export function delegationApproachToJSON(object) {
    switch (object) {
        case DelegationApproach.DELEGATION_APPROACH_UNSPECIFIED:
            return "DELEGATION_APPROACH_UNSPECIFIED";
        case DelegationApproach.DELEGATION_APPROACH_SESSION_KEY:
            return "DELEGATION_APPROACH_SESSION_KEY";
        case DelegationApproach.DELEGATION_APPROACH_SIGNED_INTENT:
            return "DELEGATION_APPROACH_SIGNED_INTENT";
        case DelegationApproach.UNRECOGNIZED:
        default:
            return "UNRECOGNIZED";
    }
}
export function delegationApproachToNumber(object) {
    switch (object) {
        case DelegationApproach.DELEGATION_APPROACH_UNSPECIFIED:
            return 0;
        case DelegationApproach.DELEGATION_APPROACH_SESSION_KEY:
            return 1;
        case DelegationApproach.DELEGATION_APPROACH_SIGNED_INTENT:
            return 2;
        case DelegationApproach.UNRECOGNIZED:
        default:
            return -1;
    }
}
/** How fees are handled for relayed transactions */
export var FeePaymentMethod;
(function (FeePaymentMethod) {
    FeePaymentMethod["FEE_PAYMENT_METHOD_UNSPECIFIED"] = "FEE_PAYMENT_METHOD_UNSPECIFIED";
    /** FEE_PAYMENT_METHOD_RELAYER_PAYS - Relayer covers all fees */
    FeePaymentMethod["FEE_PAYMENT_METHOD_RELAYER_PAYS"] = "FEE_PAYMENT_METHOD_RELAYER_PAYS";
    /** FEE_PAYMENT_METHOD_PRINCIPAL_PAYS - Deducted from principal's account */
    FeePaymentMethod["FEE_PAYMENT_METHOD_PRINCIPAL_PAYS"] = "FEE_PAYMENT_METHOD_PRINCIPAL_PAYS";
    /** FEE_PAYMENT_METHOD_SPONSOR_PAYS - Third-party sponsor covers fees */
    FeePaymentMethod["FEE_PAYMENT_METHOD_SPONSOR_PAYS"] = "FEE_PAYMENT_METHOD_SPONSOR_PAYS";
    FeePaymentMethod["UNRECOGNIZED"] = "UNRECOGNIZED";
})(FeePaymentMethod || (FeePaymentMethod = {}));
export function feePaymentMethodFromJSON(object) {
    switch (object) {
        case 0:
        case "FEE_PAYMENT_METHOD_UNSPECIFIED":
            return FeePaymentMethod.FEE_PAYMENT_METHOD_UNSPECIFIED;
        case 1:
        case "FEE_PAYMENT_METHOD_RELAYER_PAYS":
            return FeePaymentMethod.FEE_PAYMENT_METHOD_RELAYER_PAYS;
        case 2:
        case "FEE_PAYMENT_METHOD_PRINCIPAL_PAYS":
            return FeePaymentMethod.FEE_PAYMENT_METHOD_PRINCIPAL_PAYS;
        case 3:
        case "FEE_PAYMENT_METHOD_SPONSOR_PAYS":
            return FeePaymentMethod.FEE_PAYMENT_METHOD_SPONSOR_PAYS;
        case -1:
        case "UNRECOGNIZED":
        default:
            return FeePaymentMethod.UNRECOGNIZED;
    }
}
export function feePaymentMethodToJSON(object) {
    switch (object) {
        case FeePaymentMethod.FEE_PAYMENT_METHOD_UNSPECIFIED:
            return "FEE_PAYMENT_METHOD_UNSPECIFIED";
        case FeePaymentMethod.FEE_PAYMENT_METHOD_RELAYER_PAYS:
            return "FEE_PAYMENT_METHOD_RELAYER_PAYS";
        case FeePaymentMethod.FEE_PAYMENT_METHOD_PRINCIPAL_PAYS:
            return "FEE_PAYMENT_METHOD_PRINCIPAL_PAYS";
        case FeePaymentMethod.FEE_PAYMENT_METHOD_SPONSOR_PAYS:
            return "FEE_PAYMENT_METHOD_SPONSOR_PAYS";
        case FeePaymentMethod.UNRECOGNIZED:
        default:
            return "UNRECOGNIZED";
    }
}
export function feePaymentMethodToNumber(object) {
    switch (object) {
        case FeePaymentMethod.FEE_PAYMENT_METHOD_UNSPECIFIED:
            return 0;
        case FeePaymentMethod.FEE_PAYMENT_METHOD_RELAYER_PAYS:
            return 1;
        case FeePaymentMethod.FEE_PAYMENT_METHOD_PRINCIPAL_PAYS:
            return 2;
        case FeePaymentMethod.FEE_PAYMENT_METHOD_SPONSOR_PAYS:
            return 3;
        case FeePaymentMethod.UNRECOGNIZED:
        default:
            return -1;
    }
}
function createBaseDelegationScope() {
    return {
        fiberIds: [],
        allowedOperations: [],
        maxGasPerTx: undefined,
        maxTotalGas: undefined,
        policyRules: undefined,
    };
}
export const DelegationScope = {
    encode(message, writer = new BinaryWriter()) {
        for (const v of message.fiberIds) {
            writer.uint32(10).string(v);
        }
        for (const v of message.allowedOperations) {
            writer.uint32(18).string(v);
        }
        if (message.maxGasPerTx !== undefined) {
            writer.uint32(24).int64(message.maxGasPerTx);
        }
        if (message.maxTotalGas !== undefined) {
            writer.uint32(32).int64(message.maxTotalGas);
        }
        if (message.policyRules !== undefined) {
            Value.encode(Value.wrap(message.policyRules), writer.uint32(42).fork()).join();
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseDelegationScope();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.fiberIds.push(reader.string());
                    continue;
                }
                case 2: {
                    if (tag !== 18) {
                        break;
                    }
                    message.allowedOperations.push(reader.string());
                    continue;
                }
                case 3: {
                    if (tag !== 24) {
                        break;
                    }
                    message.maxGasPerTx = longToNumber(reader.int64());
                    continue;
                }
                case 4: {
                    if (tag !== 32) {
                        break;
                    }
                    message.maxTotalGas = longToNumber(reader.int64());
                    continue;
                }
                case 5: {
                    if (tag !== 42) {
                        break;
                    }
                    message.policyRules = Value.unwrap(Value.decode(reader, reader.uint32()));
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            fiberIds: globalThis.Array.isArray(object?.fiberIds)
                ? object.fiberIds.map((e) => globalThis.String(e))
                : globalThis.Array.isArray(object?.fiber_ids)
                    ? object.fiber_ids.map((e) => globalThis.String(e))
                    : [],
            allowedOperations: globalThis.Array.isArray(object?.allowedOperations)
                ? object.allowedOperations.map((e) => globalThis.String(e))
                : globalThis.Array.isArray(object?.allowed_operations)
                    ? object.allowed_operations.map((e) => globalThis.String(e))
                    : [],
            maxGasPerTx: isSet(object.maxGasPerTx)
                ? globalThis.Number(object.maxGasPerTx)
                : isSet(object.max_gas_per_tx)
                    ? globalThis.Number(object.max_gas_per_tx)
                    : undefined,
            maxTotalGas: isSet(object.maxTotalGas)
                ? globalThis.Number(object.maxTotalGas)
                : isSet(object.max_total_gas)
                    ? globalThis.Number(object.max_total_gas)
                    : undefined,
            policyRules: isSet(object?.policyRules)
                ? object.policyRules
                : isSet(object?.policy_rules)
                    ? object.policy_rules
                    : undefined,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.fiberIds?.length) {
            obj.fiberIds = message.fiberIds;
        }
        if (message.allowedOperations?.length) {
            obj.allowedOperations = message.allowedOperations;
        }
        if (message.maxGasPerTx !== undefined) {
            obj.maxGasPerTx = Math.round(message.maxGasPerTx);
        }
        if (message.maxTotalGas !== undefined) {
            obj.maxTotalGas = Math.round(message.maxTotalGas);
        }
        if (message.policyRules !== undefined) {
            obj.policyRules = message.policyRules;
        }
        return obj;
    },
    create(base) {
        return DelegationScope.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseDelegationScope();
        message.fiberIds = object.fiberIds?.map((e) => e) || [];
        message.allowedOperations = object.allowedOperations?.map((e) => e) || [];
        message.maxGasPerTx = object.maxGasPerTx ?? undefined;
        message.maxTotalGas = object.maxTotalGas ?? undefined;
        message.policyRules = object.policyRules ?? undefined;
        return message;
    },
};
function createBaseDelegationAuthority() {
    return {
        delegationId: "",
        principalAddress: "",
        delegateAddress: "",
        scope: undefined,
        approach: DelegationApproach.DELEGATION_APPROACH_UNSPECIFIED,
        expiresAt: undefined,
        nonce: 0,
        principalSignature: "",
        metadata: undefined,
    };
}
export const DelegationAuthority = {
    encode(message, writer = new BinaryWriter()) {
        if (message.delegationId !== "") {
            writer.uint32(10).string(message.delegationId);
        }
        if (message.principalAddress !== "") {
            writer.uint32(18).string(message.principalAddress);
        }
        if (message.delegateAddress !== "") {
            writer.uint32(26).string(message.delegateAddress);
        }
        if (message.scope !== undefined) {
            DelegationScope.encode(message.scope, writer.uint32(34).fork()).join();
        }
        if (message.approach !== DelegationApproach.DELEGATION_APPROACH_UNSPECIFIED) {
            writer.uint32(40).int32(delegationApproachToNumber(message.approach));
        }
        if (message.expiresAt !== undefined) {
            Timestamp.encode(toTimestamp(message.expiresAt), writer.uint32(50).fork()).join();
        }
        if (message.nonce !== 0) {
            writer.uint32(56).int64(message.nonce);
        }
        if (message.principalSignature !== "") {
            writer.uint32(66).string(message.principalSignature);
        }
        if (message.metadata !== undefined) {
            Struct.encode(Struct.wrap(message.metadata), writer.uint32(74).fork()).join();
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseDelegationAuthority();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.delegationId = reader.string();
                    continue;
                }
                case 2: {
                    if (tag !== 18) {
                        break;
                    }
                    message.principalAddress = reader.string();
                    continue;
                }
                case 3: {
                    if (tag !== 26) {
                        break;
                    }
                    message.delegateAddress = reader.string();
                    continue;
                }
                case 4: {
                    if (tag !== 34) {
                        break;
                    }
                    message.scope = DelegationScope.decode(reader, reader.uint32());
                    continue;
                }
                case 5: {
                    if (tag !== 40) {
                        break;
                    }
                    message.approach = delegationApproachFromJSON(reader.int32());
                    continue;
                }
                case 6: {
                    if (tag !== 50) {
                        break;
                    }
                    message.expiresAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
                    continue;
                }
                case 7: {
                    if (tag !== 56) {
                        break;
                    }
                    message.nonce = longToNumber(reader.int64());
                    continue;
                }
                case 8: {
                    if (tag !== 66) {
                        break;
                    }
                    message.principalSignature = reader.string();
                    continue;
                }
                case 9: {
                    if (tag !== 74) {
                        break;
                    }
                    message.metadata = Struct.unwrap(Struct.decode(reader, reader.uint32()));
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            delegationId: isSet(object.delegationId)
                ? globalThis.String(object.delegationId)
                : isSet(object.delegation_id)
                    ? globalThis.String(object.delegation_id)
                    : "",
            principalAddress: isSet(object.principalAddress)
                ? globalThis.String(object.principalAddress)
                : isSet(object.principal_address)
                    ? globalThis.String(object.principal_address)
                    : "",
            delegateAddress: isSet(object.delegateAddress)
                ? globalThis.String(object.delegateAddress)
                : isSet(object.delegate_address)
                    ? globalThis.String(object.delegate_address)
                    : "",
            scope: isSet(object.scope) ? DelegationScope.fromJSON(object.scope) : undefined,
            approach: isSet(object.approach)
                ? delegationApproachFromJSON(object.approach)
                : DelegationApproach.DELEGATION_APPROACH_UNSPECIFIED,
            expiresAt: isSet(object.expiresAt)
                ? fromJsonTimestamp(object.expiresAt)
                : isSet(object.expires_at)
                    ? fromJsonTimestamp(object.expires_at)
                    : undefined,
            nonce: isSet(object.nonce) ? globalThis.Number(object.nonce) : 0,
            principalSignature: isSet(object.principalSignature)
                ? globalThis.String(object.principalSignature)
                : isSet(object.principal_signature)
                    ? globalThis.String(object.principal_signature)
                    : "",
            metadata: isObject(object.metadata) ? object.metadata : undefined,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.delegationId !== "") {
            obj.delegationId = message.delegationId;
        }
        if (message.principalAddress !== "") {
            obj.principalAddress = message.principalAddress;
        }
        if (message.delegateAddress !== "") {
            obj.delegateAddress = message.delegateAddress;
        }
        if (message.scope !== undefined) {
            obj.scope = DelegationScope.toJSON(message.scope);
        }
        if (message.approach !== DelegationApproach.DELEGATION_APPROACH_UNSPECIFIED) {
            obj.approach = delegationApproachToJSON(message.approach);
        }
        if (message.expiresAt !== undefined) {
            obj.expiresAt = message.expiresAt.toISOString();
        }
        if (message.nonce !== 0) {
            obj.nonce = Math.round(message.nonce);
        }
        if (message.principalSignature !== "") {
            obj.principalSignature = message.principalSignature;
        }
        if (message.metadata !== undefined) {
            obj.metadata = message.metadata;
        }
        return obj;
    },
    create(base) {
        return DelegationAuthority.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseDelegationAuthority();
        message.delegationId = object.delegationId ?? "";
        message.principalAddress = object.principalAddress ?? "";
        message.delegateAddress = object.delegateAddress ?? "";
        message.scope = (object.scope !== undefined && object.scope !== null)
            ? DelegationScope.fromPartial(object.scope)
            : undefined;
        message.approach = object.approach ?? DelegationApproach.DELEGATION_APPROACH_UNSPECIFIED;
        message.expiresAt = object.expiresAt ?? undefined;
        message.nonce = object.nonce ?? 0;
        message.principalSignature = object.principalSignature ?? "";
        message.metadata = object.metadata ?? undefined;
        return message;
    },
};
function createBaseSessionKey() {
    return {
        delegationId: "",
        sessionPublicKey: "",
        sessionExpiresAt: undefined,
        sessionScope: undefined,
        authorizationSignature: "",
    };
}
export const SessionKey = {
    encode(message, writer = new BinaryWriter()) {
        if (message.delegationId !== "") {
            writer.uint32(10).string(message.delegationId);
        }
        if (message.sessionPublicKey !== "") {
            writer.uint32(18).string(message.sessionPublicKey);
        }
        if (message.sessionExpiresAt !== undefined) {
            Timestamp.encode(toTimestamp(message.sessionExpiresAt), writer.uint32(26).fork()).join();
        }
        if (message.sessionScope !== undefined) {
            DelegationScope.encode(message.sessionScope, writer.uint32(34).fork()).join();
        }
        if (message.authorizationSignature !== "") {
            writer.uint32(42).string(message.authorizationSignature);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseSessionKey();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.delegationId = reader.string();
                    continue;
                }
                case 2: {
                    if (tag !== 18) {
                        break;
                    }
                    message.sessionPublicKey = reader.string();
                    continue;
                }
                case 3: {
                    if (tag !== 26) {
                        break;
                    }
                    message.sessionExpiresAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
                    continue;
                }
                case 4: {
                    if (tag !== 34) {
                        break;
                    }
                    message.sessionScope = DelegationScope.decode(reader, reader.uint32());
                    continue;
                }
                case 5: {
                    if (tag !== 42) {
                        break;
                    }
                    message.authorizationSignature = reader.string();
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            delegationId: isSet(object.delegationId)
                ? globalThis.String(object.delegationId)
                : isSet(object.delegation_id)
                    ? globalThis.String(object.delegation_id)
                    : "",
            sessionPublicKey: isSet(object.sessionPublicKey)
                ? globalThis.String(object.sessionPublicKey)
                : isSet(object.session_public_key)
                    ? globalThis.String(object.session_public_key)
                    : "",
            sessionExpiresAt: isSet(object.sessionExpiresAt)
                ? fromJsonTimestamp(object.sessionExpiresAt)
                : isSet(object.session_expires_at)
                    ? fromJsonTimestamp(object.session_expires_at)
                    : undefined,
            sessionScope: isSet(object.sessionScope)
                ? DelegationScope.fromJSON(object.sessionScope)
                : isSet(object.session_scope)
                    ? DelegationScope.fromJSON(object.session_scope)
                    : undefined,
            authorizationSignature: isSet(object.authorizationSignature)
                ? globalThis.String(object.authorizationSignature)
                : isSet(object.authorization_signature)
                    ? globalThis.String(object.authorization_signature)
                    : "",
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.delegationId !== "") {
            obj.delegationId = message.delegationId;
        }
        if (message.sessionPublicKey !== "") {
            obj.sessionPublicKey = message.sessionPublicKey;
        }
        if (message.sessionExpiresAt !== undefined) {
            obj.sessionExpiresAt = message.sessionExpiresAt.toISOString();
        }
        if (message.sessionScope !== undefined) {
            obj.sessionScope = DelegationScope.toJSON(message.sessionScope);
        }
        if (message.authorizationSignature !== "") {
            obj.authorizationSignature = message.authorizationSignature;
        }
        return obj;
    },
    create(base) {
        return SessionKey.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseSessionKey();
        message.delegationId = object.delegationId ?? "";
        message.sessionPublicKey = object.sessionPublicKey ?? "";
        message.sessionExpiresAt = object.sessionExpiresAt ?? undefined;
        message.sessionScope = (object.sessionScope !== undefined && object.sessionScope !== null)
            ? DelegationScope.fromPartial(object.sessionScope)
            : undefined;
        message.authorizationSignature = object.authorizationSignature ?? "";
        return message;
    },
};
function createBaseSignedIntent() {
    return {
        delegationId: "",
        transaction: undefined,
        intentNonce: 0,
        intentExpiresAt: undefined,
        executionConditions: undefined,
        intentSignature: "",
    };
}
export const SignedIntent = {
    encode(message, writer = new BinaryWriter()) {
        if (message.delegationId !== "") {
            writer.uint32(10).string(message.delegationId);
        }
        if (message.transaction !== undefined) {
            Struct.encode(Struct.wrap(message.transaction), writer.uint32(18).fork()).join();
        }
        if (message.intentNonce !== 0) {
            writer.uint32(24).int64(message.intentNonce);
        }
        if (message.intentExpiresAt !== undefined) {
            Timestamp.encode(toTimestamp(message.intentExpiresAt), writer.uint32(34).fork()).join();
        }
        if (message.executionConditions !== undefined) {
            Value.encode(Value.wrap(message.executionConditions), writer.uint32(42).fork()).join();
        }
        if (message.intentSignature !== "") {
            writer.uint32(50).string(message.intentSignature);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseSignedIntent();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.delegationId = reader.string();
                    continue;
                }
                case 2: {
                    if (tag !== 18) {
                        break;
                    }
                    message.transaction = Struct.unwrap(Struct.decode(reader, reader.uint32()));
                    continue;
                }
                case 3: {
                    if (tag !== 24) {
                        break;
                    }
                    message.intentNonce = longToNumber(reader.int64());
                    continue;
                }
                case 4: {
                    if (tag !== 34) {
                        break;
                    }
                    message.intentExpiresAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
                    continue;
                }
                case 5: {
                    if (tag !== 42) {
                        break;
                    }
                    message.executionConditions = Value.unwrap(Value.decode(reader, reader.uint32()));
                    continue;
                }
                case 6: {
                    if (tag !== 50) {
                        break;
                    }
                    message.intentSignature = reader.string();
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            delegationId: isSet(object.delegationId)
                ? globalThis.String(object.delegationId)
                : isSet(object.delegation_id)
                    ? globalThis.String(object.delegation_id)
                    : "",
            transaction: isObject(object.transaction) ? object.transaction : undefined,
            intentNonce: isSet(object.intentNonce)
                ? globalThis.Number(object.intentNonce)
                : isSet(object.intent_nonce)
                    ? globalThis.Number(object.intent_nonce)
                    : 0,
            intentExpiresAt: isSet(object.intentExpiresAt)
                ? fromJsonTimestamp(object.intentExpiresAt)
                : isSet(object.intent_expires_at)
                    ? fromJsonTimestamp(object.intent_expires_at)
                    : undefined,
            executionConditions: isSet(object?.executionConditions)
                ? object.executionConditions
                : isSet(object?.execution_conditions)
                    ? object.execution_conditions
                    : undefined,
            intentSignature: isSet(object.intentSignature)
                ? globalThis.String(object.intentSignature)
                : isSet(object.intent_signature)
                    ? globalThis.String(object.intent_signature)
                    : "",
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.delegationId !== "") {
            obj.delegationId = message.delegationId;
        }
        if (message.transaction !== undefined) {
            obj.transaction = message.transaction;
        }
        if (message.intentNonce !== 0) {
            obj.intentNonce = Math.round(message.intentNonce);
        }
        if (message.intentExpiresAt !== undefined) {
            obj.intentExpiresAt = message.intentExpiresAt.toISOString();
        }
        if (message.executionConditions !== undefined) {
            obj.executionConditions = message.executionConditions;
        }
        if (message.intentSignature !== "") {
            obj.intentSignature = message.intentSignature;
        }
        return obj;
    },
    create(base) {
        return SignedIntent.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseSignedIntent();
        message.delegationId = object.delegationId ?? "";
        message.transaction = object.transaction ?? undefined;
        message.intentNonce = object.intentNonce ?? 0;
        message.intentExpiresAt = object.intentExpiresAt ?? undefined;
        message.executionConditions = object.executionConditions ?? undefined;
        message.intentSignature = object.intentSignature ?? "";
        return message;
    },
};
function createBaseDelegationRevocation() {
    return { delegationId: "", reason: undefined, nonce: 0, revocationSignature: "", revokedAt: undefined };
}
export const DelegationRevocation = {
    encode(message, writer = new BinaryWriter()) {
        if (message.delegationId !== "") {
            writer.uint32(10).string(message.delegationId);
        }
        if (message.reason !== undefined) {
            writer.uint32(18).string(message.reason);
        }
        if (message.nonce !== 0) {
            writer.uint32(24).int64(message.nonce);
        }
        if (message.revocationSignature !== "") {
            writer.uint32(34).string(message.revocationSignature);
        }
        if (message.revokedAt !== undefined) {
            Timestamp.encode(toTimestamp(message.revokedAt), writer.uint32(42).fork()).join();
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseDelegationRevocation();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.delegationId = reader.string();
                    continue;
                }
                case 2: {
                    if (tag !== 18) {
                        break;
                    }
                    message.reason = reader.string();
                    continue;
                }
                case 3: {
                    if (tag !== 24) {
                        break;
                    }
                    message.nonce = longToNumber(reader.int64());
                    continue;
                }
                case 4: {
                    if (tag !== 34) {
                        break;
                    }
                    message.revocationSignature = reader.string();
                    continue;
                }
                case 5: {
                    if (tag !== 42) {
                        break;
                    }
                    message.revokedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            delegationId: isSet(object.delegationId)
                ? globalThis.String(object.delegationId)
                : isSet(object.delegation_id)
                    ? globalThis.String(object.delegation_id)
                    : "",
            reason: isSet(object.reason) ? globalThis.String(object.reason) : undefined,
            nonce: isSet(object.nonce) ? globalThis.Number(object.nonce) : 0,
            revocationSignature: isSet(object.revocationSignature)
                ? globalThis.String(object.revocationSignature)
                : isSet(object.revocation_signature)
                    ? globalThis.String(object.revocation_signature)
                    : "",
            revokedAt: isSet(object.revokedAt)
                ? fromJsonTimestamp(object.revokedAt)
                : isSet(object.revoked_at)
                    ? fromJsonTimestamp(object.revoked_at)
                    : undefined,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.delegationId !== "") {
            obj.delegationId = message.delegationId;
        }
        if (message.reason !== undefined) {
            obj.reason = message.reason;
        }
        if (message.nonce !== 0) {
            obj.nonce = Math.round(message.nonce);
        }
        if (message.revocationSignature !== "") {
            obj.revocationSignature = message.revocationSignature;
        }
        if (message.revokedAt !== undefined) {
            obj.revokedAt = message.revokedAt.toISOString();
        }
        return obj;
    },
    create(base) {
        return DelegationRevocation.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseDelegationRevocation();
        message.delegationId = object.delegationId ?? "";
        message.reason = object.reason ?? undefined;
        message.nonce = object.nonce ?? 0;
        message.revocationSignature = object.revocationSignature ?? "";
        message.revokedAt = object.revokedAt ?? undefined;
        return message;
    },
};
function createBaseRelayedTransaction() {
    return {
        transaction: undefined,
        delegationProof: undefined,
        gasConfig: undefined,
        relayerAddress: "",
        relayerSignature: "",
    };
}
export const RelayedTransaction = {
    encode(message, writer = new BinaryWriter()) {
        if (message.transaction !== undefined) {
            Struct.encode(Struct.wrap(message.transaction), writer.uint32(10).fork()).join();
        }
        switch (message.delegationProof?.$case) {
            case "sessionKeyProof":
                SessionKeyProof.encode(message.delegationProof.sessionKeyProof, writer.uint32(18).fork()).join();
                break;
            case "signedIntentProof":
                SignedIntentProof.encode(message.delegationProof.signedIntentProof, writer.uint32(26).fork()).join();
                break;
        }
        if (message.gasConfig !== undefined) {
            GasConfig.encode(message.gasConfig, writer.uint32(34).fork()).join();
        }
        if (message.relayerAddress !== "") {
            writer.uint32(42).string(message.relayerAddress);
        }
        if (message.relayerSignature !== "") {
            writer.uint32(50).string(message.relayerSignature);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseRelayedTransaction();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.transaction = Struct.unwrap(Struct.decode(reader, reader.uint32()));
                    continue;
                }
                case 2: {
                    if (tag !== 18) {
                        break;
                    }
                    message.delegationProof = {
                        $case: "sessionKeyProof",
                        sessionKeyProof: SessionKeyProof.decode(reader, reader.uint32()),
                    };
                    continue;
                }
                case 3: {
                    if (tag !== 26) {
                        break;
                    }
                    message.delegationProof = {
                        $case: "signedIntentProof",
                        signedIntentProof: SignedIntentProof.decode(reader, reader.uint32()),
                    };
                    continue;
                }
                case 4: {
                    if (tag !== 34) {
                        break;
                    }
                    message.gasConfig = GasConfig.decode(reader, reader.uint32());
                    continue;
                }
                case 5: {
                    if (tag !== 42) {
                        break;
                    }
                    message.relayerAddress = reader.string();
                    continue;
                }
                case 6: {
                    if (tag !== 50) {
                        break;
                    }
                    message.relayerSignature = reader.string();
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            transaction: isObject(object.transaction) ? object.transaction : undefined,
            delegationProof: isSet(object.sessionKeyProof)
                ? { $case: "sessionKeyProof", sessionKeyProof: SessionKeyProof.fromJSON(object.sessionKeyProof) }
                : isSet(object.session_key_proof)
                    ? { $case: "sessionKeyProof", sessionKeyProof: SessionKeyProof.fromJSON(object.session_key_proof) }
                    : isSet(object.signedIntentProof)
                        ? { $case: "signedIntentProof", signedIntentProof: SignedIntentProof.fromJSON(object.signedIntentProof) }
                        : isSet(object.signed_intent_proof)
                            ? { $case: "signedIntentProof", signedIntentProof: SignedIntentProof.fromJSON(object.signed_intent_proof) }
                            : undefined,
            gasConfig: isSet(object.gasConfig)
                ? GasConfig.fromJSON(object.gasConfig)
                : isSet(object.gas_config)
                    ? GasConfig.fromJSON(object.gas_config)
                    : undefined,
            relayerAddress: isSet(object.relayerAddress)
                ? globalThis.String(object.relayerAddress)
                : isSet(object.relayer_address)
                    ? globalThis.String(object.relayer_address)
                    : "",
            relayerSignature: isSet(object.relayerSignature)
                ? globalThis.String(object.relayerSignature)
                : isSet(object.relayer_signature)
                    ? globalThis.String(object.relayer_signature)
                    : "",
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.transaction !== undefined) {
            obj.transaction = message.transaction;
        }
        if (message.delegationProof?.$case === "sessionKeyProof") {
            obj.sessionKeyProof = SessionKeyProof.toJSON(message.delegationProof.sessionKeyProof);
        }
        else if (message.delegationProof?.$case === "signedIntentProof") {
            obj.signedIntentProof = SignedIntentProof.toJSON(message.delegationProof.signedIntentProof);
        }
        if (message.gasConfig !== undefined) {
            obj.gasConfig = GasConfig.toJSON(message.gasConfig);
        }
        if (message.relayerAddress !== "") {
            obj.relayerAddress = message.relayerAddress;
        }
        if (message.relayerSignature !== "") {
            obj.relayerSignature = message.relayerSignature;
        }
        return obj;
    },
    create(base) {
        return RelayedTransaction.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseRelayedTransaction();
        message.transaction = object.transaction ?? undefined;
        switch (object.delegationProof?.$case) {
            case "sessionKeyProof": {
                if (object.delegationProof?.sessionKeyProof !== undefined && object.delegationProof?.sessionKeyProof !== null) {
                    message.delegationProof = {
                        $case: "sessionKeyProof",
                        sessionKeyProof: SessionKeyProof.fromPartial(object.delegationProof.sessionKeyProof),
                    };
                }
                break;
            }
            case "signedIntentProof": {
                if (object.delegationProof?.signedIntentProof !== undefined && object.delegationProof?.signedIntentProof !== null) {
                    message.delegationProof = {
                        $case: "signedIntentProof",
                        signedIntentProof: SignedIntentProof.fromPartial(object.delegationProof.signedIntentProof),
                    };
                }
                break;
            }
        }
        message.gasConfig = (object.gasConfig !== undefined && object.gasConfig !== null)
            ? GasConfig.fromPartial(object.gasConfig)
            : undefined;
        message.relayerAddress = object.relayerAddress ?? "";
        message.relayerSignature = object.relayerSignature ?? "";
        return message;
    },
};
function createBaseSessionKeyProof() {
    return { sessionKey: undefined, transactionSignature: "" };
}
export const SessionKeyProof = {
    encode(message, writer = new BinaryWriter()) {
        if (message.sessionKey !== undefined) {
            SessionKey.encode(message.sessionKey, writer.uint32(10).fork()).join();
        }
        if (message.transactionSignature !== "") {
            writer.uint32(18).string(message.transactionSignature);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseSessionKeyProof();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.sessionKey = SessionKey.decode(reader, reader.uint32());
                    continue;
                }
                case 2: {
                    if (tag !== 18) {
                        break;
                    }
                    message.transactionSignature = reader.string();
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            sessionKey: isSet(object.sessionKey)
                ? SessionKey.fromJSON(object.sessionKey)
                : isSet(object.session_key)
                    ? SessionKey.fromJSON(object.session_key)
                    : undefined,
            transactionSignature: isSet(object.transactionSignature)
                ? globalThis.String(object.transactionSignature)
                : isSet(object.transaction_signature)
                    ? globalThis.String(object.transaction_signature)
                    : "",
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.sessionKey !== undefined) {
            obj.sessionKey = SessionKey.toJSON(message.sessionKey);
        }
        if (message.transactionSignature !== "") {
            obj.transactionSignature = message.transactionSignature;
        }
        return obj;
    },
    create(base) {
        return SessionKeyProof.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseSessionKeyProof();
        message.sessionKey = (object.sessionKey !== undefined && object.sessionKey !== null)
            ? SessionKey.fromPartial(object.sessionKey)
            : undefined;
        message.transactionSignature = object.transactionSignature ?? "";
        return message;
    },
};
function createBaseSignedIntentProof() {
    return { signedIntent: undefined, conditionProof: undefined };
}
export const SignedIntentProof = {
    encode(message, writer = new BinaryWriter()) {
        if (message.signedIntent !== undefined) {
            SignedIntent.encode(message.signedIntent, writer.uint32(10).fork()).join();
        }
        if (message.conditionProof !== undefined) {
            Value.encode(Value.wrap(message.conditionProof), writer.uint32(18).fork()).join();
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseSignedIntentProof();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.signedIntent = SignedIntent.decode(reader, reader.uint32());
                    continue;
                }
                case 2: {
                    if (tag !== 18) {
                        break;
                    }
                    message.conditionProof = Value.unwrap(Value.decode(reader, reader.uint32()));
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            signedIntent: isSet(object.signedIntent)
                ? SignedIntent.fromJSON(object.signedIntent)
                : isSet(object.signed_intent)
                    ? SignedIntent.fromJSON(object.signed_intent)
                    : undefined,
            conditionProof: isSet(object?.conditionProof)
                ? object.conditionProof
                : isSet(object?.condition_proof)
                    ? object.condition_proof
                    : undefined,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.signedIntent !== undefined) {
            obj.signedIntent = SignedIntent.toJSON(message.signedIntent);
        }
        if (message.conditionProof !== undefined) {
            obj.conditionProof = message.conditionProof;
        }
        return obj;
    },
    create(base) {
        return SignedIntentProof.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseSignedIntentProof();
        message.signedIntent = (object.signedIntent !== undefined && object.signedIntent !== null)
            ? SignedIntent.fromPartial(object.signedIntent)
            : undefined;
        message.conditionProof = object.conditionProof ?? undefined;
        return message;
    },
};
function createBaseGasConfig() {
    return { gasLimit: 0, gasPrice: undefined, paymentMethod: FeePaymentMethod.FEE_PAYMENT_METHOD_UNSPECIFIED };
}
export const GasConfig = {
    encode(message, writer = new BinaryWriter()) {
        if (message.gasLimit !== 0) {
            writer.uint32(8).int64(message.gasLimit);
        }
        if (message.gasPrice !== undefined) {
            writer.uint32(16).int64(message.gasPrice);
        }
        if (message.paymentMethod !== FeePaymentMethod.FEE_PAYMENT_METHOD_UNSPECIFIED) {
            writer.uint32(24).int32(feePaymentMethodToNumber(message.paymentMethod));
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseGasConfig();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 8) {
                        break;
                    }
                    message.gasLimit = longToNumber(reader.int64());
                    continue;
                }
                case 2: {
                    if (tag !== 16) {
                        break;
                    }
                    message.gasPrice = longToNumber(reader.int64());
                    continue;
                }
                case 3: {
                    if (tag !== 24) {
                        break;
                    }
                    message.paymentMethod = feePaymentMethodFromJSON(reader.int32());
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            gasLimit: isSet(object.gasLimit)
                ? globalThis.Number(object.gasLimit)
                : isSet(object.gas_limit)
                    ? globalThis.Number(object.gas_limit)
                    : 0,
            gasPrice: isSet(object.gasPrice)
                ? globalThis.Number(object.gasPrice)
                : isSet(object.gas_price)
                    ? globalThis.Number(object.gas_price)
                    : undefined,
            paymentMethod: isSet(object.paymentMethod)
                ? feePaymentMethodFromJSON(object.paymentMethod)
                : isSet(object.payment_method)
                    ? feePaymentMethodFromJSON(object.payment_method)
                    : FeePaymentMethod.FEE_PAYMENT_METHOD_UNSPECIFIED,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.gasLimit !== 0) {
            obj.gasLimit = Math.round(message.gasLimit);
        }
        if (message.gasPrice !== undefined) {
            obj.gasPrice = Math.round(message.gasPrice);
        }
        if (message.paymentMethod !== FeePaymentMethod.FEE_PAYMENT_METHOD_UNSPECIFIED) {
            obj.paymentMethod = feePaymentMethodToJSON(message.paymentMethod);
        }
        return obj;
    },
    create(base) {
        return GasConfig.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseGasConfig();
        message.gasLimit = object.gasLimit ?? 0;
        message.gasPrice = object.gasPrice ?? undefined;
        message.paymentMethod = object.paymentMethod ?? FeePaymentMethod.FEE_PAYMENT_METHOD_UNSPECIFIED;
        return message;
    },
};
function createBaseCreateDelegation() {
    return { delegation: undefined };
}
export const CreateDelegation = {
    encode(message, writer = new BinaryWriter()) {
        if (message.delegation !== undefined) {
            DelegationAuthority.encode(message.delegation, writer.uint32(10).fork()).join();
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseCreateDelegation();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.delegation = DelegationAuthority.decode(reader, reader.uint32());
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return { delegation: isSet(object.delegation) ? DelegationAuthority.fromJSON(object.delegation) : undefined };
    },
    toJSON(message) {
        const obj = {};
        if (message.delegation !== undefined) {
            obj.delegation = DelegationAuthority.toJSON(message.delegation);
        }
        return obj;
    },
    create(base) {
        return CreateDelegation.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseCreateDelegation();
        message.delegation = (object.delegation !== undefined && object.delegation !== null)
            ? DelegationAuthority.fromPartial(object.delegation)
            : undefined;
        return message;
    },
};
function createBaseRegisterSessionKey() {
    return { sessionKey: undefined };
}
export const RegisterSessionKey = {
    encode(message, writer = new BinaryWriter()) {
        if (message.sessionKey !== undefined) {
            SessionKey.encode(message.sessionKey, writer.uint32(10).fork()).join();
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseRegisterSessionKey();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.sessionKey = SessionKey.decode(reader, reader.uint32());
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            sessionKey: isSet(object.sessionKey)
                ? SessionKey.fromJSON(object.sessionKey)
                : isSet(object.session_key)
                    ? SessionKey.fromJSON(object.session_key)
                    : undefined,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.sessionKey !== undefined) {
            obj.sessionKey = SessionKey.toJSON(message.sessionKey);
        }
        return obj;
    },
    create(base) {
        return RegisterSessionKey.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseRegisterSessionKey();
        message.sessionKey = (object.sessionKey !== undefined && object.sessionKey !== null)
            ? SessionKey.fromPartial(object.sessionKey)
            : undefined;
        return message;
    },
};
function createBaseSubmitSignedIntent() {
    return { signedIntent: undefined };
}
export const SubmitSignedIntent = {
    encode(message, writer = new BinaryWriter()) {
        if (message.signedIntent !== undefined) {
            SignedIntent.encode(message.signedIntent, writer.uint32(10).fork()).join();
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseSubmitSignedIntent();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.signedIntent = SignedIntent.decode(reader, reader.uint32());
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            signedIntent: isSet(object.signedIntent)
                ? SignedIntent.fromJSON(object.signedIntent)
                : isSet(object.signed_intent)
                    ? SignedIntent.fromJSON(object.signed_intent)
                    : undefined,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.signedIntent !== undefined) {
            obj.signedIntent = SignedIntent.toJSON(message.signedIntent);
        }
        return obj;
    },
    create(base) {
        return SubmitSignedIntent.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseSubmitSignedIntent();
        message.signedIntent = (object.signedIntent !== undefined && object.signedIntent !== null)
            ? SignedIntent.fromPartial(object.signedIntent)
            : undefined;
        return message;
    },
};
function createBaseRevokeDelegation() {
    return { revocation: undefined };
}
export const RevokeDelegation = {
    encode(message, writer = new BinaryWriter()) {
        if (message.revocation !== undefined) {
            DelegationRevocation.encode(message.revocation, writer.uint32(10).fork()).join();
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseRevokeDelegation();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.revocation = DelegationRevocation.decode(reader, reader.uint32());
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return { revocation: isSet(object.revocation) ? DelegationRevocation.fromJSON(object.revocation) : undefined };
    },
    toJSON(message) {
        const obj = {};
        if (message.revocation !== undefined) {
            obj.revocation = DelegationRevocation.toJSON(message.revocation);
        }
        return obj;
    },
    create(base) {
        return RevokeDelegation.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseRevokeDelegation();
        message.revocation = (object.revocation !== undefined && object.revocation !== null)
            ? DelegationRevocation.fromPartial(object.revocation)
            : undefined;
        return message;
    },
};
function createBaseSubmitRelayedTransaction() {
    return { relayedTx: undefined };
}
export const SubmitRelayedTransaction = {
    encode(message, writer = new BinaryWriter()) {
        if (message.relayedTx !== undefined) {
            RelayedTransaction.encode(message.relayedTx, writer.uint32(10).fork()).join();
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseSubmitRelayedTransaction();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.relayedTx = RelayedTransaction.decode(reader, reader.uint32());
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            relayedTx: isSet(object.relayedTx)
                ? RelayedTransaction.fromJSON(object.relayedTx)
                : isSet(object.relayed_tx)
                    ? RelayedTransaction.fromJSON(object.relayed_tx)
                    : undefined,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.relayedTx !== undefined) {
            obj.relayedTx = RelayedTransaction.toJSON(message.relayedTx);
        }
        return obj;
    },
    create(base) {
        return SubmitRelayedTransaction.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseSubmitRelayedTransaction();
        message.relayedTx = (object.relayedTx !== undefined && object.relayedTx !== null)
            ? RelayedTransaction.fromPartial(object.relayedTx)
            : undefined;
        return message;
    },
};
function toTimestamp(date) {
    const seconds = Math.trunc(date.getTime() / 1000);
    const nanos = (date.getTime() % 1000) * 1000000;
    return { seconds, nanos };
}
function fromTimestamp(t) {
    let millis = (t.seconds || 0) * 1000;
    millis += (t.nanos || 0) / 1000000;
    return new globalThis.Date(millis);
}
function fromJsonTimestamp(o) {
    if (o instanceof globalThis.Date) {
        return o;
    }
    else if (typeof o === "string") {
        return new globalThis.Date(o);
    }
    else {
        return fromTimestamp(Timestamp.fromJSON(o));
    }
}
function longToNumber(int64) {
    const num = globalThis.Number(int64.toString());
    if (num > globalThis.Number.MAX_SAFE_INTEGER) {
        throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
    }
    if (num < globalThis.Number.MIN_SAFE_INTEGER) {
        throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
    }
    return num;
}
function isObject(value) {
    return typeof value === "object" && value !== null;
}
function isSet(value) {
    return value !== null && value !== undefined;
}
